# R10.3 v1.1 implementation — in progress, NOT released

Baseline: `5ddfabdcfa4a97f6eba543f2b57165f7d6f90795` (main).
Design: `AtomicSkillGraph_R10.3_v1.1_执行表达整合_修复设计交付包`.
Latest status: implementation pushed to main as `a480c9f`; full regression
1673 passed, corrected-copy real T1 passed. Dev16 remains running; not formally
released. Entries below retain the chronological earlier states.
Development is isolated in `runs/r103_development`; historical banks and running
experiments are not modified. No formal experiment is authorized by this change.

## Acceptance sequence

- [x] M1 complete typed Atomic/Tool/Implementation identity and bounded proofs;
  register-time deduplication, source-specific replay certificates.
- [x] M5 new-bank protocol, indexes/capsules, transaction, freeze/checkpoint/digest.
- [x] M2 execution-attributed support, lifecycle, route-first Candidate exposure.
- [x] M3 bounded two-source E1 sidecar and original admission/replay chain.
- [x] M4 native-tool-first reversible policy projection and prompt replacement.
- [ ] AUT/CAU production regressions; all pytest; preflight; finite real validation.
- [x] Reports, six fresh formal configs, diagnostic interventions, main push.

## Baseline

1549 tests passed. One reporting test failed because a Windows absolute worktree
gitdir is not readable by WSL git. Changed only the worktree's local `.git` pointer
to a relative path, not production source or any test expectation.

No effect-size/token claim or formal release is implied by unit tests.

## Continuation checkpoint (2026-09-22)

All source changes remain in this isolated worktree. Main has not been changed
or pushed. No real API validation or formal training has been started here.

- Full regression at the start of this continuation: **1624 passed / 92.34s**,
  `runs/r103_validation/current_full.log`.
- Complete joint Implementation proofs now carry their chosen Tool-role proofs.
  Runtime admission maps each original Tool replay case through a complete Tool
  proof; Tool support and Implementation support remain separate. An interim
  route-only admission grouping was corrected to preserve P03: identical Tools
  may receive support while different Implementation mappings cannot share it.
- New additional-admission boundary preserves ordinary E1 facts, avoids duplicate
  proposals/validations, and permits a later valid sidecar to add a missing
  validation without overwriting a rejected earlier occurrence.
- Sidecar checks independent manifest identity itself, including retry aliases.
- Parent Trace snapshots can be read after freeze/relocation; corrupt originals
  still fail closed. Full learning-source normalization/Atomicizer verification
  is run before freeze and on opening a readonly R10.3 system.
- Shared capability boundary descriptions connect E1, draft, Builder and schema.
- Registry/Tool alignment scans share the 100000-state allowance.
- C11/C01/C10/C00/A0 deployment masks and same-current-state old presentation are
  implemented at exposure/automatic entry boundaries; formal configs reject
  their use. Diagnostic driver/source manifest validation remains to be wired.
- Targeted route/intervention/sidecar regression: **20 passed / 12.52s**,
  `runs/r103_validation/diagnostic_boundaries.log`.
- A previous targeted run had 67 passes and one exception-message regex mismatch;
  updated the assertion to match the new shared source reader; integrity behavior
  itself was unchanged. Do not count that earlier run as all passed.

Still required: failure-admission idempotence; complete route/display coverage;
source indexes/transaction/freeze audits; diagnostic and dev16 drivers; remaining
acceptance-matrix regressions; full final pytest and real finite T1–T6/dev16;
honest implementation/removal/mechanism reports and main integration/push.

## Continuation checkpoint: reporting and locked validation

- `current_full_6.log`: 1658 passed / 104.10 s; `current_full_7.log`:
  1660 passed / 102.49 s. Final copy retests include the additional unchanged-count
  assertion for declared-order Implementation identity buckets.
- Added full original source replay/admission two-source tests (one real
  controlled Builder request, two independent source identities, success and
  second-case rejection, no fake online credit). Both passed.
- Added route dedup/reserved Candidate slot/single quota tests, readonly helper
  display checks, multi-Tool order matching, strict frozen metadata verification,
  report unknown reasoning and all-task paired denominators.
- Found and fixed the matching-index inconsistency: Implementation matching
  used declared order but the retrieval bucket used list storage order.
- R10.3 structured diagnostics now flow into the original experiment report.
  Identity snapshots are detached from later matches; no authority changes.
- Preflight passed with fixed 120 tasks (20 each). The prior deterministic full
  smoke passed at 1651 tests; newer full suites are listed separately.
- The locked copy is `/home/yangchengyu/asg_r103_validation_0vlGjI`; a supervisor
  runs full pytest, then real T1/dev16, then predeclared samebank two-task
  C11/C01/C10/A0. Formal train/test is not part of this script.
- A first isolated-copy test attempt lacked Git metadata: 1659 passed, one
  existing diagnostic failed `git rev-parse HEAD`. Added the base worktree Git
  pointer, did not weaken any assertion, and restarted before any API call.
- Main still unchanged/unpushed. Implementation/removal/method/intervention
  reports exist and explicitly mark real acceptance pending.

- Latest complete worktree regression: **1670 passed / 106.42 s**,
  `current_full_8.log`. Ten extra assertions test semantic predicate obligations
  and original positive-credit gates. The locked live implementation is unchanged.
- Locked copy full regression: **1660 passed / 104.78 s**. Real T1 **passed** in
  **100.57 s**, real provider attempts in successor segment **0**, provider usage
  counts 4→4. Both real provider capability probes passed.
- dev16 started, empty-bank first task is in ordinary E1/Builder/E2 learning.
  No full-task result or T2 success is claimed before its Trace is committed.

## Real source roundtrip correction

- First task committed successfully. Second task stopped in historical source
  verification, not in the environment or provider. All failed evidence and
  352524 known task tokens remain in the first fixed copy.
- Fixed persisted dictionary spans in the Normalizer. Fixed singleton
  revalidation's batch-local occurrence ordinal while retaining exact original
  source ID/index consistency and full evidence equality.
- Three new production Normalizer/source-store tests pass; the nine actual
  saved source capsules also pass a read-only verification, no model calls.
- Full regression after these fixes: **1673 passed / 108.01 s**;
  `git diff --check` passed. Fresh fixed copy:
  `/home/yangchengyu/asg_r103_validation_v2_n435n2`.
- First dev's nine ordinary occurrences were single-action. Do not label that
  run a T2 multi-step success or a completed dev16 acceptance.
