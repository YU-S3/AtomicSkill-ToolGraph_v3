# R10.2.1 删除与迁移审计

只改变 main 实现；历史配置/Trace/证书作为只读证据保留，不构成兼容执行开关。

| 原逻辑/调用者 | 迁移后的唯一边界 | 验证 |
|---|---|---|
| `RuntimeBudget.node_action_budget`/节点 token 计数与拦截；System 节点/任务剩余取 min | Task 100 动作 + 六在线桶共享 UsageLedger 600K；节点只归因 | A01–A07 |
| 35 动作衍生节点 turn 上限/旧默认回填 | task action cap 的协议安全上限；短步一次语义决定，单独 protocol-turn 错误 | A07 |
| E1 固定 entity/concrete input 与固定 semantic output | native v2 ParameterSpec，类型/认证/时点验证，canonical 和 Builder 原样传递 | B01–B07/E04–E05 |
| 当前普通 E1 可无声明进入 action-only legacy reducer | 默认 Atomicizer 拒绝 legacy；`FailureAtomicSourceReplay` 显式启用，只认证源回放，当前内部晋升使用 v2 | G03、provisional/failure tests |
| “occurrence 有一个 Tool span 即全部覆盖” | canonical action ID 与实际 committed Tool ancestry 覆盖差集 | C01–C05 |
| `extract_novel_seeded_success` 无效开关 | 从默认/新配置/生产构造器及当前 smoke fixture 删除；旧历史 YAML 不重写 | 源码搜索、C01–C04 |
| interpreter 见 terminal 一律退出 | 原 PC 纯尾部；下一 ACTION/Tool 前停止，不跳跃 | D01–D05 |
| 当前 started partial 写 future skip；图未完整 RETURN 一律否定部署 | 当前 TERMINAL_PARTIAL，后缀 SKIPPED；真实图终止贡献与工具完整度独立 | D06–D09 |
| `_trace_has_graph_execution_evidence` 平行近似判断 | 删除无调用 helper，统一使用 `composite_deployment_evidence` 的实际归属判断 | D07–D09 |
| Tool final effect 未过仍 completed；Atomic 真覆盖串行 Tool 失败 | completed && 全部 Tool 成功 && Atomic 认证；事务只能进一步拒绝，保留第一错误 | E01–E03 |
| Publisher 固定 entity/CONCRETE、raw fallback、全体验证升权 | 三结果类型实际 certified maps；整批 JSON/角色/状态一致性校验后发布；Support/helper/冷启动/图入口同步迁移 | E04–E05 |
| ARGUMENT_EXISTS 比证据总数量 | 每个不同 JSON 类型和值均有当前有效证据；重复同值可重用同一证据 | E06 |
| Dynamic 仅 environment/status，无父 Atomic 禁止自工具化 | TaskConsumer 只是来源标识；共用主动工具/Builder/事务/R1；真实 task scope 观察 | F01–F06 |

没有恢复自动 Support、ownership 调度、缺参自动求解、首 ACTION 反推入口或 Composite 巨型 Tool。没有新增按 task ID、family、对象、位置、动作名称选择生产路线/预算/成功条件的分支。固定 task 列表和菜单仅在 `experiments/` 明示验收 fixture；生产端仅依赖 authored contract、公共接口与验证证据。

测试迁移：保留原有效 safety/rollback/identity/replay/治理测试；旧终止预期按新合同修正并补充反例。旧 legacy 单元输入显式标注 `legacy_source_replay=True`，普通 E1 系统单元输入迁移为 v2，不给生产验证器打通过补丁；历史原始 Trace JSON 未改写。Windows 临时 SQLite 测试使用 context manager 关闭连接，不修改生产数据库语义。

新的 `task_runtime.py` 不保存新知识库、不创建第二个资源池；typed_boundary 与 retired-limit 统计是验证/投影，均不作为新调度器。后者不得用于提前截断执行。
