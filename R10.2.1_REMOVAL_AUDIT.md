# R10.2.1 删除与迁移审计

只改变 main 实现；历史配置/Trace/证书作为只读证据保留，不构成兼容执行开关。

| 原逻辑/调用者 | 迁移后的唯一边界 | 验证 |
|---|---|---|
| `RuntimeBudget.node_action_budget`/节点 token 计数与拦截；System 节点/任务剩余取 min | Task 100 动作 + 六在线桶共享 UsageLedger 600K；节点只归因 | A01–A07 |
| 35 动作衍生节点 turn 上限/旧默认回填 | task action cap 的协议安全上限；短步一次语义决定，单独 protocol-turn 错误 | A07 |
| E1 固定 entity/concrete input 与固定 semantic output | native v2 ParameterSpec，类型/认证/时点验证，canonical 和 Builder 原样传递 | B01–B07/E04–E05 |
| 当前普通 E1 可无声明进入 action-only legacy reducer | 默认 Atomicizer 拒绝 legacy；`FailureAtomicSourceReplay` 显式启用，只认证源回放，当前内部晋升使用 v2 | G03、provisional/failure tests |
| “occurrence 有一个 Tool span 即全部覆盖” | canonical action ID 与实际 committed Tool ancestry 覆盖差集 | C01–C05 |
| `extract_novel_seeded_success` 无效开关 | 从默认/新配置/生产构造器及当前 smoke fixture 删除；旧历史 YAML 不重写 | 源码搜索、C01–C04 |
| interpreter 见 terminal 一律退出 | 原 PC 纯尾部；下一 ACTION/Tool 前停止，不跳跃 | D01–D05 |
| 当前 started partial 写 future skip；图未完整 RETURN 一律否定部署 | 当前 TERMINAL_PARTIAL，后缀 SKIPPED；真实图终止贡献与工具完整度独立 | D06–D09 |
| `_trace_has_graph_execution_evidence` 平行近似判断 | 删除无调用 helper，统一使用 `composite_deployment_evidence` 的实际归属判断 | D07–D09 |
| Tool final effect 未过仍 completed；Atomic 真覆盖串行 Tool 失败 | completed && 全部 Tool 成功 && Atomic 认证；事务只能进一步拒绝，保留第一错误 | E01–E03 |
| Publisher 固定 entity/CONCRETE、raw fallback、全体验证升权 | 三结果类型实际 certified maps；整批 JSON/角色/状态一致性校验后发布；Support/helper/冷启动/图入口同步迁移 | E04–E05 |
| ARGUMENT_EXISTS 比证据总数量 | 每个不同 JSON 类型和值均有当前有效证据；重复同值可重用同一证据 | E06 |
| Dynamic 仅 environment/status，无父 Atomic 禁止自工具化 | TaskConsumer 只是来源标识；共用主动工具/Builder/事务/R1；真实 task scope 观察 | F01–F06 |

没有恢复自动 Support、ownership 调度、缺参自动求解、首 ACTION 反推入口或 Composite 巨型 Tool。没有新增按 task ID、family、对象、位置、动作名称选择生产路线/预算/成功条件的分支。固定 task 列表和菜单仅在 `experiments/` 及独立验收附件中明示为 fixture；生产端仅依赖 authored contract、公共接口与验证证据。

测试迁移：保留原有效 safety/rollback/identity/replay/治理测试；旧终止预期按新合同修正并补充反例。旧 legacy 单元输入显式标注 `legacy_source_replay=True`，普通 E1 系统单元输入迁移为 v2，不给生产验证器打通过补丁；历史原始 Trace JSON 未改写。Windows 临时 SQLite 测试使用 context manager 关闭连接，不修改生产数据库语义。

新的 `task_runtime.py` 不保存新知识库、不创建第二个资源池；typed_boundary 与 retired-limit 统计是验证/投影，均不作为新调度器。后者不得用于提前截断执行。

## 调用者复核

- 资源入口：`system.py::_runtime_remaining_tokens/_runtime_session/_shared_tool_builder_tokens` 和 `agents/session.py` 共读 task Ledger；`RuntimeBudget.consume_action` 只检查 global actions。生产目录无 `consume_llm`、`remaining_node_actions`、`node_action_budget` 执行入口。`runtime/r10_metrics.py` 的历史展示字段 `configured_max_total_tokens_per_node=None` 不参与执行；`r1021_metrics.py` 仅旁观旧阈值。
- typed 输出的实际生产调用者：`runtime/orchestrator.py` 的图节点与 Atomic resolution 发布、`cold_start_executor.py` 的 continuation 发布、`task_runtime.py` 的 Dynamic helper 发布都传入同次认证 maps；`support_request.py` 在输入转移前检查真实输出类型/权限。共同出口为 `BindingStore.publish_validated_outputs` 和 `EvidenceStore.add_validated_tool_output`，没有另一条 raw fallback。
- 验证结果来自 `validation/atomic_validator.py`，经 `runtime/node_executor.py` 或 `implementation_runner.py` 传递；`invocation_transaction.py` 的事务失败会清空输出认证，不能将部分成功重新发布。E01–E06 正反例覆盖这些共同边界。
- `EvidenceStore` 的 ARGUMENT_EXISTS/ARGUMENT_CONCRETE 逐值判断仍由 `invocation_compiler.py` 的实际约束引用；不是新增自动绑定器。
- 额外真实验收驱动与只读审计脚本位于 `runs/r1021_validation/`，单独记录 SHA 并作为实验附件保留；其中声明的固定题目、准备轨迹和参数化 fixture 不进入生产包，也不作为自主方法效果。

## 模块增删统计

口径：`git diff 024957a..4679cd2 --numstat`，测试单独汇总，治理目录内原测试计入该模块。数字是补丁行数，不代表运行路径数量或质量。

| 模块 | 新增 | 删除 |
|---|---:|---:|
| configs | 289 | 7 |
| experiments（不含其 tests） | 236 | 56 |
| agents | 86 | 69 |
| core | 18 | 0 |
| evolution | 288 | 48 |
| governance | 60 | 48 |
| runtime | 469 | 451 |
| system.py | 31 | 24 |
| tooling | 3 | 0 |
| validation | 61 | 7 |
| tests / experiments tests | 998 | 314 |

接口说明修正：E1 的 `input_provenance_refs[formal].source_role` 匹配所引用 authority 的 `role`，不是该 authority 的祖先 `source_role` 审计字段。仅澄清 prompt/schema，保留实际角色、值、类型、入口时点及 scope 验证；旧措辞断言迁移，新反例证明祖先角色不能代替当前端口。

## R10.2.1-Final 删除与替换审计

基线 `4fcf70a` → 最终执行 `1bdc1d3`，只修改主线 F1–F4 边界：

| 删除/替换 | 保留 |
|---|---|
| 首 selected support 代替声明输入入口 | `event_start`、每个 local 使用前 authority、typed 输出与真实 witness |
| 同 revision 历史 validation 全局 false 否决和无范围历史 witness 授信 | 新提案自身完整验证、历史原记录 |
| E1 parser 对单项交叉字段时间范围提前抛整个 batch | native/schema 校验、一次 exclusive→inclusive 转换、subset 独立拒绝 |
| 同 owner 多短步必须被一个大 Span 包住 | 全 envelope owner、真实 parent/范围/learnable/revision/rollback；不造长 Span |
| E1/E2/E2R 同 provider conversation 假设 | 一个逻辑 Extractor、各阶段一次、共享一次协议 repair 与 task 三桶额度 |
| 新阶段分配完整离线额度、Extractor 漏接动态共享 guard | Ledger/task cursor；当前余量分配及每请求 min(local,shared)；完整收费 |
| E1 authority 顶层重复副本 | canonical_trace 中唯一完整 authority |
| Node/Dynamic 重复且损失嵌套信息的反馈实现 | 共用小型白名单投影、实际调用参数/revision、typed 输出、公开错误文本 |
| 任意实体已有 authority 就必须外置输入的泛化措辞 | 明确声明入口、local 使用时点、fresh 相对显式 inputs、相同身份不得伪装 fresh |

没有恢复自动 SupportClosure、ownership 交权、缺参求解或第二个 Runtime/Builder；没有按任务/家族/对象/位置选择生产路线。
`REPLAY_AUTHORITY_VERSION=tool_replay_r1021_final_v1`，避免把旧验证结论当作新权限证书。
测试只迁移过时接口/预期；保留 orphan、冻结隔离、typed publisher、终止必需动作、Candidate 与费用正反例。
合成 witness-owner 测试的旧重叠 revision fixture 改为相邻真实转换、刻意重复 opaque witness，仍断言真实 witness ownership 拒绝；没有放宽生产连续性验证。
独立验收驱动位于 `runs/r1021_final_validation`，不进入生产调度，不把声明 Active/helper fixture 冒充自主学习。
## R10.2.1-H 联合精简

- 替换 ContextBuilder 中 E1/Builder 长角色前缀，不在尾部追加第二份规则手册。
- E2/E2R 共用一个阶段前缀，修复只追加本阶段必要错误信息。
- 删除 Runtime step / Dynamic task 重复 one-step 指令，保留 scope 各自说明及一份公共策略。
- 系统角色提示压缩为职责名称，详细规则留在阶段消息；planner/maintenance 未改。
- 删除 Dynamic task frame 内与根完全相同的 runtime automation interface；原值未被截断或改写。
- draft schema 不再复制完整 fresh-output 规则，改为指向原公开 interface；长文本唯一，执行验证未改。
- Tool ACTION 不再复用图专用引用 enum；只在该字段换成 Tool 已支持的三个 kind，图绑定和深层 AST 不扩展。
- 所有有效旧测试保留；移除的是重复文本位置断言，替换为唯一规则来源/实际请求及原 validator 正反例。
- 没有删除预算、typed 输出、entry、replay、terminal、orphan、证书、Candidate/Frozen 检查。
- 没有新增 provider strict 模式、checker、schema 自动修补、额外 retry、自动 Support 或 benchmark 特判。
## R10.2.1-I 补充

- 删除 System 对旧顶层状态数组的两段读取，删除 success_evolution 的 after_facts 空末态传参。
- 替换 Builder SOURCE AND REPLAY 段和 E1 三处数组下标描述，不追加第二份规则。
- 证据投影不再默认 accepted=true/revision=0；必需字段由 source helper 在请求前检查。正负事实共用原标准字段白名单。
- 补入既有公开 collection_sources，未新增 DSL、replay 入口、证书权限、自动路由或新预算。
- 删除三个只有 substring 断言的 R7 prompt 测试；保留原语义边界、坏提案、source authority、Frozen、预算与 typed 发布回归。
- 历史/预算隔离 fixture 的补全不修改历史 JSON，也不移除原先针对错误处理的有效断言。
