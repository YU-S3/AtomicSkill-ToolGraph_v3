# R10.2 实现与验收

状态：代码重构与确定性回归已完成，真实 API 定向验收及固定 16 题开发集尚待结算，**未放行正式 Full-120**。

## 实现范围

- 一个图节点入口、一个 Agent 节点循环；Agent 接手后 explore/helper/trial/失败反馈均不恢复自动执行。
- 只使用 Task/Constant/显式 DataFlow/已验证 Agent 交付；身份与关系时效分离，未知参数不由 catalog 搜索补选。
- ToolProposal v2 明确入口合同、真实参数类型、逐 Tool 入口校验；纯 preflight 后才进入事务提交；删除首 ACTION 推断。
- Tool RETURN、显式 Agent 完成候选及声明的 input-identity 共用结果验证；验证 witness 保留到输出发布；辅助只交付父输入。
- E1 指导持久化并进入实际节点请求，软参考、可缺省、portable；声明的编号角色不误判成 episode literal。
- 恢复 fresh RuntimeStep 中被拒绝调用的未启动审计；保留生命周期去重，不记成 Tool intrinsic failure。
- 新 R10.2 Full-120/Frozen-134 配置注册，任务、模型、reasoning、预算、阈值不漂移；旧配置不可静默启动新引擎。

## 确定性证据

当前全量 pytest：**1388 passed / 49.95 秒**（`/tmp/r102_sixteenth.log`）。新增针对性测试覆盖 C01/C03/C05/C07/C09/C10、V01/V02/V05/V07/V08、D01/D02/D03、L02、R02；其余事务/Repeat/输出/晋升/Frozen/预算边界沿用并迁移有效回归。`git diff --check` 通过。两个新正式配置的 `_validate_formal_config` 均通过。

确定性 fixture 不是自主实验成绩；测试数量不替代真实 T1/T3 和 fixed16 放行门槛。

## 真实验收（待运行/结算）

- T1：`experiments.r102_dataflow_check`，真实 Builder 生成两份工具，Agent 选具体参数，真实 RETURN→双显式边→后节点无额外 Agent 自动执行。其 Active 状态是声明测试输入，不计作生命周期证据。
- T2 / fixed16：`experiments.run_v3_r102_targeted --mode dev16`，固定顺序 2,3,15,16,28,34,36,40,45,46,53,57,1,7,12,31，fresh bank，逐题完整结算。
- T3：同一 driver `--mode chain`，独立 fresh bank，声明合同与首次菜单，值/路线/程序均由真实 Agent/Builder 生成；自然跨任务 promotion、Candidate 显式部署、Active 后显式复用。不得恢复自动 Support 作为通过证据。

每轮固定 code hash，保留全部失败及用量；输出 `declared_manifest.json`、`provider_capability_manifest.json`、`progress.json`、`all_usage.json`、`summary.json`、`trace_store/traces` 和 bank。报告将补入最终 commit/hash、实测路径及逐题结果。

## 放行限制

当前尚无 R10.2 fixed16 或正式成绩，不宣称达到 128/134。旧 bank 不用于本轮训练或续跑。历史实验产物保持不变，移除的代码与旧测试可由 Git 恢复。清理详情见 `R10.2_REMOVAL_AUDIT.md`。
