# R10.2 实现与验收报告

## 结论与固定版本

R10.2 工程验收完成，可以启动新的空库 seed42 Full-120 → Frozen-134。**本次没有启动正式实验，也没有证明达到 128/134 的效果目标。** 固定开发集完整运行 16 题，official won 为 **13/16（81.25%）**；三个失败如实保留，不修改预算、身份规则或搜索策略来使它们通过。

- 审查基线：`64942eb620360abe51ec62843c19278d64d0d6ec`。
- 最终实现提交：`9466d56ef0a4b631fab3642674d595d47c1ca3ec`；其后的本报告提交仅更新 Markdown，不改变执行文件指纹。
- 固定 execution code hash：`8d19ae7b66efcd8704465ab9e1841df069b7054ffe33138dcfef7909b92c7c55`。
- 实际验证 checkout：`/home/yangchengyu/asg_r102_typed_entry_ctTyEP/code`；全过程未修改。
- 完整证据备份根目录：`D:/T3S_exp/AtomicSkill-ToolGraph_v3/runs/r102_validation_20260917/validated_9466d56/`，下文的证据相对路径均基于此目录。原始 WSL 结果在 `/home/yangchengyu/asg_r102_typed_entry_ctTyEP/results/`。
- 全量 pytest：**1394 passed，42.82 秒**，`pytest.log`。两个新正式配置校验通过，`formal_config_checks.json`。T1/T3/dev16 的真实 provider capability 均通过，manifest 的 code hash 与上述一致。`git diff --check` 通过。

## 修改项与回归

| 修改项 | 实现边界 | 测试依据 |
| --- | --- | --- |
| 一个图节点入口、一个 Agent 循环 | 单次入口检查；graph bootstrap 一次；Agent 接手后 explore/helper/trial/失败反馈均不触发自动接管 | `test_r102_execution.py`：C01/C03/C05/C07/C09/C10；真实 T1/T2 |
| 仅读取给定参数 | Task/Constant/显式 DataFlow/已认证 Agent 交付；不从 catalog 求唯一候选 | 同上 D01–D03；现有绑定/Repeat 回归 |
| 身份与关系时效分离 | 保留验证过的具体 identity；关系与 affordance 按当前 revision 重验 | `test_r101_boundaries.py`、R10.2 execution 回归 |
| ToolProposal v2 | 作者显式 entry contract、真实类型 schema、逐 Tool 入口；删除首 ACTION 入口推断 | `test_r102_contract_boundaries.py`、V01/V02/L02 |
| 纯 preflight 与统一事务 | 检查不提交绑定，实际调用 capture 后再提交；失败恢复、不退款 | V07、`test_r92_runtime_trial_boundaries.py`、既有 invocation transaction 测试 |
| 显式完成候选 | Tool RETURN、Agent candidate outputs、声明的 input-identity 共用结果验证；不求解缺失输出 | V04/V05/V08、`test_repeat_blocks.py` |
| Skill guideline | E1→canonical→存储→真实 Runtime payload；软参考，portable、有界；缺省标记 | `test_deterministic_fullchain.py`、`test_r102_provisional.py`、Runtime prompt 测试 |
| 主动辅助与自工具化 | 显式父输入映射、联合 witness、父完成后正常晋升；无自动 Support | `test_r921_self_tooling_route.py`、真实 T3 |
| 生命周期/证书/Frozen | 入口变化改变 executable identity；拒绝候选不发布持久证书；普通阈值和 Frozen 隔离不变 | `test_r101_replay_publication.py`、`test_r922_replay_certificates.py`、`test_frozen_guards.py` |
| 新正式配置和诊断审计 | 注册 R10.2，旧协议执行明确拒绝；usage 落盘，maintenance→audit→freeze | `test_r102_diagnostics.py`、`test_r101_formal_boundaries.py` |

删除/迁移明细及仍有效的安全测试见 [R10.2_REMOVAL_AUDIT.md](R10.2_REMOVAL_AUDIT.md)。未为了测试数守恒保留旧自动 Support、ownership 交权或双 Runtime 控制器；历史 Trace/bank/报告未删除。未改 baseline 分支。

本次没有按任务 ID、对象类别、地点名称或动作名称增加 Runtime 策略；没有手写搜索路线、额外同义词表或目标位置答案。定向测试中的父合同/菜单/Active fixture 明确标注，只位于验收 driver，不进入生产策略。保留的类型、身份、Repeat、显式映射验证是安全合同，不是解题策略。

### 实测发现并修复的入口解析错误

中间 fixed16 的 task2 Tool 明确声明 `agent.holds($object)` 和 `agent.at_location($destination)`。持久化形式为 `{"kind":"skill_input","source_role":...}`，但 `SemanticPredicate` 不自动转换 args。旧入口读取将该字典当字面值传入 Harness，导致真实事实已满足仍被误拒。

`9466d56` 仅在 `check_tool_entry` 边界将合法声明反序列化为 `BindingExpression`；不改 Harness、合同或参数选择。三个 JSON 往返正反例覆盖 structured input、constant、`$role`：正确事实通过，无事实/错误身份/过期位置仍拒绝，纯检查不改世界。修复前两个 structured 用例失败，修复后全量通过。

`../entry_boundary_actual_replay_v2.json` 从原始不可变 task2 replay case 的四步前缀重建实际环境：旧解析 false、新解析 true、错误对象 false。它仅证明入口解析修复，不冒充完整 Tool replay PASS；不改原 bank/Trace。首次复现已生成结果但清理调用不存在的 adapter.close，日志保留；修正审计脚本为关闭其 TextWorld 环境后，独立 v2 复现完整退出 0。日志为 `entry_boundary_actual_v2.log`。

## 真实定向验收（不是自主开发集成绩）

### T1：显式 RETURN → DataFlow → 下游自动执行

`t1/summary.json`：**passed=true**，47.45 秒，26,173 tokens。两个真实 Builder、一次真实参数选择、一次 Runtime 调用；两节点后的累计 provider 事件数 `[4,4]`，所以下游额外调用为 0。

Trace：`t1/trace_store/traces/trace_2e267c412b8a4946b187c038798d29a8.json`。真实 GO_TO 返回 object/location，经两个显式边传入下游；作者 entry contract 通过后执行 EXAMINE，Harness 的 `object.observed` witness 通过，`direct_autonomous_success`，未归属动作 0。这里的 Active 状态是声明 fixture，不计作自然晋升证据。

### T2：task3 缺参后由 Agent 完整执行

使用下面 fixed16 的真实 task3，而不是 copied bank 或预置导航答案：`stored_composite`，official won=true，TaskContract agreement=true，204.33 秒，9 次 Runtime Agent 调用，9 个环境动作，自动图节点/辅助/恢复回放均为 0；170,787 tokens（含任务内学习）。

Trace：`dev16/trace_store/traces/trace_83146fdb3bf8450b86e5514e15da0227.json`。入口原因依次为 graph bootstrap、无实现、仅 Candidate、无实现。Agent 自行探索、选择实例与动作、显式提交完成候选，两个 Repeat 身份分别验证，最终 official done/won 后无额外确认调用。全部动作来自 Agent 或实际调用 Tool，未授权动作 0。

### T3：自工具化 → Candidate → Active → Agent 显式复用

`t3/summary.json`：8/8 official won；完整 wall time **1,890.74 秒（31.51 分钟）**，任务时间合计 1,863.40 秒；**1,343,859 tokens**，含学习和末批维护，不含独立 capability probe。`t3_audit.json` 对账 90 个唯一 usage event，全部与不可变 Trace 一致。

这是独立空库的声明父合同/首次菜单验收；具体值、路线、工具程序由真实 Agent/Builder 产生。不得将其当成自主学习开发集 8/8。

| task | 目标资产调用前状态 | 实际链路 |
| --- | --- | --- |
| 0 | 无 | draft、真实 Builder、独立 R1、父节点/任务完成，来源观察 1 |
| 1 | 无 | 第二来源 R1、父节点/任务完成；跨来源 replay 后 Candidate |
| 3 | Candidate | Agent 显式调用目标辅助、真实返回/联合 witness/父输入交付成功；Builder=0 |
| 4 | Candidate | 第二次正常显式部署成功；不降低晋升阈值 |
| 7 | Candidate | 本题辅助 `tool_ir_action_unavailable` 失败，父任务随后完成；不计成功复用。正常维护依据此前两次成功部署变为 Active |
| 10 | Active | Agent 选了其他辅助，映射权威不足被拒；任务完成，但不计目标 Tool 复用 |
| 12 | Active | 首次其他辅助映射被拒；随后选择正确目标辅助并显式映射，复用成功、Builder=0、父节点/任务完成 |
| 18 | Active | 正确目标辅助显式复用成功、Builder=0、父节点/任务完成 |

目标资产：

- `skill://atomic_e3eb67090ec62106bfdaaa63@1.0.0`
- `skill://impl_f0761a1ceacfc9bf18bd7402@1.0.0`
- `tool://tool_37b4b057f263dd8d2e9494d9@1.0.0`

Active 成功复用 Trace：`t3/trace_store/traces/trace_645bbe7ef2df40008af936653d7d72e2.json`（task12）。输入 `input_000=mug`，真实 RETURN `output_000=mug_2, output_001=cabinet_11`；Agent 显式映射到父 object/source；`entity.discovered_at(entity=mug_2, location=cabinet_11)` 的同一联合关系 witness 验证后交付，不凭同名/同类型自动接线。task18 的成功 Trace 为 `trace_e420f01eff14422b8e2e9aa83a0e76c4`。

普通末批维护无 pending proposal，artifact audit 通过，freeze 源 digest 不变：`70e93bceb57cf97e435fc8c94c369e30e3d2aa7da5432af5cd69000c2c693477`。末批维护的 7,467 tokens 已计入总量。Frozen 运行隔离由既有真实生产路径回归覆盖；本诊断的实际末步是 freeze，不冒充已经跑过正式 held-out 测试。

## 固定开发集逐题结果

声明于执行前的 task ID/顺序：`2,3,15,16,28,34,36,40,45,46,53,57,1,7,12,31`，逐项匹配 `train_120.json` 的 env_index/gamefile hash。代码、模型、reasoning 和预算固定；一次新空库完整运行，不挑题、不拼接旧前缀、不重跑失败题。

完整结果：`dev16/summary.json`、`dev16/progress.json`、`dev16_audit.json`。总 wall **3,082.35 秒（51.37 分钟）**；任务合计 3,000.56 秒，余量含初始化/probe/末批维护/freeze。官方成功 **13/16**；无基础设施失败，无未归属世界动作，无成本缺失。TaskContract agreement 单列诊断，没有重新建立 strict-success 门槛。

### 执行统计

Agent 为 Runtime provider 调用次数；辅助为 Agent 主动调用次数（含拒绝/失败，不等于成功部署）；RT 为本题 Runtime automation 请求次数。自动列只统计无需 Agent 的完整图节点，不把 Agent 选中 Tool 内部的零 LLM 动作混进去。

| task | official won | graph source | Agent | 自动 | 辅助 | RT | 环境动作 | 恢复回放 | 秒 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2 | true | full_dynamic | 8 | 0 | 0 | 0 | 8 | 0 | 242.67 |
| 3 | true | stored_composite | 9 | 0 | 0 | 0 | 9 | 0 | 204.33 |
| 15 | true | stored_composite | 21 | 0 | 0 | 0 | 21 | 0 | 121.64 |
| 16 | false | stored_composite | 17 | 0 | 0 | 0 | 14 | 0 | 273.57 |
| 28 | true | stored_composite | 10 | 0 | 0 | 0 | 9 | 0 | 97.13 |
| 34 | true | full_dynamic | 6 | 0 | 0 | 0 | 6 | 0 | 244.66 |
| 36 | true | full_dynamic | 41 | 0 | 0 | 0 | 41 | 0 | 259.88 |
| 40 | false | full_dynamic | 58 | 0 | 0 | 0 | 56 | 0 | 253.31 |
| 45 | true | stored_composite | 17 | 0 | 2 | 0 | 17 | 0 | 227.05 |
| 46 | true | stored_composite | 12 | 0 | 3 | 0 | 10 | 0 | 214.63 |
| 53 | true | stored_composite | 11 | 0 | 1 | 0 | 8 | 0 | 158.98 |
| 57 | false | stored_composite | 11 | 0 | 0 | 0 | 8 | 0 | 194.26 |
| 1 | true | atomic_composition | 14 | 0 | 0 | 0 | 14 | 0 | 75.24 |
| 7 | true | full_dynamic | 13 | 0 | 0 | 0 | 13 | 0 | 255.35 |
| 12 | true | stored_composite | 7 | 0 | 1 | 0 | 6 | 0 | 68.20 |
| 31 | true | stored_composite | 8 | 0 | 2 | 0 | 6 | 0 | 109.67 |

### 每个自动入口未执行的原因

以下按实际入口顺序列出 `revision:原因`；没有自动重入。`C×2` 表示该次入口两份候选均为 Candidate，不是两次执行。详细 occurrence/ref 保留在 `progress.json.entry_decisions`。B=graph bootstrap；N=无实现；C=Candidate 不自动执行；S=给定参数缺少必填项导致 schema 拒绝；G=现有值尚未认证为 concrete；P=Atomic 当前 precondition 不满足。除 B/N 外，这些入口的汇总 reason 为 `no_ready_active_implementation`。

| task | 实际入口原因 |
| --- | --- |
| 2 / 34 / 36 / 40 / 7 | full_dynamic，无图入口记录 |
| 3 | r0:B；r2:N；r5:C×2；r7:N |
| 15 | r0:B；r15:C；r17:C×2；r19:C |
| 16 | r0:B；r2:C；r5:C×2，随后第三 occurrence 预算耗尽 |
| 28 | r0:B；r3:C；r5:C×2；r7:C |
| 45 | r0:B；r2:C；r5:S×2；r15:C |
| 46 | r0:B；r7:C |
| 53 | r0:B；r2:C；r4:S×2；r6:C |
| 57 | r0:B，当前 occurrence 预算耗尽 |
| 1 | r0:B；r12:C |
| 12 / 31 | r0:B；r1:G×2；r2:C；r4:P |

S 的原始码为 `runtime_agent_schema_error`，但这里来自纯自动 preflight，不是说 Agent 已提交非法调用；对应公开 snapshot 明确缺 `object/source`。G 的原始码为 `runtime_binding_not_concrete`，P 为 `runtime_preconditions_unsatisfied`。缺参继续交 Agent，不从 catalog 自动赋值。

### Token 与不可变 Trace

以下含各题归属的规划、运行和学习费用；non-reasoning completion 仅在 provider 明确 reasoning 包含于 completion 时相减。不是只列成功任务或只列 Runtime。

| task | prompt | reasoning | non-reasoning completion | total | Trace ID |
| --- | ---: | ---: | ---: | ---: | --- |
| 2 | 126670 | 40059 | 9819 | 176548 | `trace_c1ca74df35ca44969271e78cb4120b7d` |
| 3 | 129079 | 36362 | 5346 | 170787 | `trace_83146fdb3bf8450b86e5514e15da0227` |
| 15 | 114589 | 18024 | 1893 | 134506 | `trace_c4c7f171778146ae91abfe8023c0e093` |
| 16 | 88246 | 50177 | 1601 | 140024 | `trace_902979d5a1e24df089b3674495ec3b3c` |
| 28 | 55344 | 10610 | 917 | 66871 | `trace_110067d9c2b84941a2c51e0253880a5c` |
| 34 | 114483 | 28462 | 5187 | 148132 | `trace_ed43408f001a4e95b6a20bde425d7253` |
| 36 | 333638 | 12533 | 6319 | 352490 | `trace_4b247f07cd2948dbab65f3d3759f8a95` |
| 40 | 298565 | 29616 | 4378 | 332559 | `trace_d236ba3a50aa4db89730819f9f81fed4` |
| 45 | 94930 | 33311 | 1613 | 129854 | `trace_d0b61146694048fc9a7c90096d5fc5fb` |
| 46 | 81919 | 29706 | 1172 | 112797 | `trace_12299993efe246e7861a96d1301abe03` |
| 53 | 57562 | 17443 | 1217 | 76222 | `trace_82611e0db2884592958d532563e59295` |
| 57 | 84870 | 22793 | 1200 | 108863 | `trace_a3b41923819b442181a105fc949ee8fb` |
| 1 | 83965 | 10752 | 2471 | 97188 | `trace_af3e1976d59e4ac684816bedecd8bca8` |
| 7 | 112915 | 37660 | 5474 | 156049 | `trace_f0afb75b3d0a49989857dd496d6d5293` |
| 12 | 39386 | 8386 | 737 | 48509 | `trace_a6d429e606c24d47a1c99e958eac7b26` |
| 31 | 44450 | 12998 | 1047 | 58495 | `trace_7c47af0ebaef461fbcc58f2238764061` |

每份文件为 `dev16/trace_store/traces/<Trace ID>.json`。逐题合计 2,309,894 tokens，另加末批维护 13,755，**全局合计 2,323,649**（prompt 1,871,814；reasoning 400,806；non-reasoning completion 51,029）。末批 Trace：`trace_d1a7818c1ae14919a48445bd55c4f209`。能力 probe 单独存档，不混入任务费用。

`dev16_audit.json` 验证 301 个唯一全局 usage event 与全部不可变 Trace 逐项完全相等，没有重复、未落 Trace 用量或未计费 Trace 调用。300K 是既定 task Runtime 池而非含 Planner/E1/E2 的全系统成本上限；例如 task36 表中大于300K不等于偷偷提高Runtime预算。

### 三个失败及边界判断

- **task16**：`runtime_node_token_budget_exhausted`。第二实例搜索期间尝试再次取用已经完成第一轮放置的 `toiletpaper_2`；显式完成候选在动作前被 `runtime_repetition_distinctness_violation` 拒绝。没有自动 Support 拉回位置，没有放宽 distinct 约束。第三 occurrence 达到原100K额度，记录失败。
- **task40**：`runtime_task_token_budget_exhausted`。full_dynamic 的58次Runtime调用、56个已接受动作，搜索/开柜/查看中存在重复访问，未完成找到并处理目标。无 Tool 入口拒绝、无自动路线、无回滚。不能通过预置目标位置或免费搜索来“修复”这类策略失败。
- **task57**：`runtime_semantic_anchor_mismatch` 后 `runtime_node_token_budget_exhausted`。正式目标 Cup，Agent 选择 `mug_1` 并调用加热实现，语义身份校验拒绝；随后搜索继续但额度耗尽。ALFWorld 原始 constants 分别声明 Cup 与 Mug，正式 game 类型为 `pick_heat_then_place_in_recep-Cup-None-Cabinet-27`，不能手写 Cup=Mug 绕过。无需修改已有正确校验。

成功题的拒绝/失败尝试同样保留在 raw Trace，不只保留最终成功。当前已完成集没有发现新系统性错接参数、伪证据、未授权动作、旧自动路径或基础设施结算错误。**这不等于保证未见场景没有缺陷，也不等于达到效果目标。**

### 冻结与限制

dev16 正常末批 maintenance 的 pending proposals 为空，artifact audit 通过，freeze 源 digest 未变：`8534692545443900762e7b3ea6f6ac828915d8e4de95f9f044e0feaa86fbcc15`。

开发集中自动完整图节点为0、主动 Runtime automation 请求为0，不能声称这16题已自然产生大量自动图复用或自工具化收益。T1证明自动显式数据流能力，T3证明正常持久化/部署/Active显式复用链；开发集存在主动辅助，但机制触发率、Frozen泛化和真实成本收益仍须正式实验测量。旧 copied-bank/不同提交的历史成本不能作为本轮严格消融对照。

## 保留的中间记录（不作为最终成绩）

- `/home/yangchengyu/asg_r102_validation_oVZjjS/`：初始 T1 缺 replay 示例、后续 OPEN 非容器 fixture 的失败均保留；后改为导航→EXAMINE 的合法 fixture，不放宽验证器。首轮缺完整 usage 的尝试明确不能声称成本完整。c16c787 的 T3/dev16 各保存5题后在任务边界停止，未混入新前缀。
- `/home/yangchengyu/asg_r102_entry_validation_HPDOxK/`（c6787ae）：补充 ToolBuilder 输入存在与证据存在的通用语义。T3 8/8、dev16 12/16完成，但审计发现上面的结构化 entry 读取缺陷，因此未放行。旧诊断 driver 遗漏末批维护，被 freeze 守卫正确阻止；独立声明的收尾使用原 checkout 正常 maintenance/audit/freeze，原 Trace 不改写。5229068 修正 driver，不改守卫。全部记录备份在 `../intermediate_c6787ae/`。
- 最终9466d56在全新目录重新完成 T1/T3/dev16，代码运行中无变更；最终成绩不采用中间结果或补结算前缀。

## 正式实验边界

只使用新配置 `configs/alfworld_train_full_120_r102_seed42.yaml` 与 `configs/alfworld_frozen_eval_134_r102_seed42.yaml`。从新目录和 empty bank 开始，train 成功及其维护/冻结审计完成后才执行配对 Frozen-134；不 resume R10/R10.1/R10.1.1 bank，不导入任何诊断资产。建议 checkout/output 放 WSL Linux 文件系统，避免历史 `/mnt/d` checkpoint rename 权限问题。运行期间固定代码；本轮仅提供命令，不代用户启动正式实验。
