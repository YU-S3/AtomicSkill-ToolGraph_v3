# R10.3 OBS-1 / OBS-2 minimal repair — 2026-09-22

Reviewed against `ef6d36ca46229661831c2519016c8c104c5852e9` and the supplied
source-conflict/observation review. Only the two proven observation defects were
changed. Source deduplication, representative ranking, sidecar accounting,
prompts, caps, budgets, contracts, validation, credit and policy are unchanged.

## Changes

- OBS-1: offline Runtime counts associate `metadata.runtime_steps.session_id`
  with the formal session producer types. Unique session/request pairs count
  decisions and protocol repairs separately. Missing sessions/audits, invalid
  audits or conflicting classifications return null with explicit partial reasons.
  No session protocol or usage bucket is renamed; old unobserved Traces stay null.
- OBS-2: transaction observation samples after the existing preflight binding
  commit. Direct runner/trial observation uses the effective preflight overlay;
  trial does not inherit bindings its runner does not use. These are read-only
  observations, not extra preflight calls or grounding mutations. A same-value
  HARNESS_EVIDENCE replacement no longer earns original DATA_FLOW credit, while
  an identical DATA_FLOW update retains it.

## Controlled tests and read-only reconciliation

CM04 now executes real compiler preflight and ImplementationRunner through the
transaction, direct and trial entrances, with unchanged, recertified, overwritten
and identical bindings. Observer on/off pairs compare requests, actions,
preflight, output/effect validations, invocation results, world state and usage.
The changed-value update is certified in a fresh compiler scope: this isolates
the pending-update boundary without bypassing the existing concrete semantic
anchor guard. No validator is stubbed to PASS. CM01 retains nonempty provider
request/context invariance coverage. CM06 verifies 3 formal producer types,
1 decision + 1 repair, deduplication, missing audit/session and old-Trace nulls.

Targeted suite: 22 passed. Full suite: **1716 passed in 128.77 seconds**.
Full log: `runs/r103_review/observation_pytest.log`.

Read-only audit output: `runs/r103_review/observation_reconciliation/`.
Reproduction script: `runs/r103_review/reconcile_observations.py`.
Original source: `/home/yangchengyu/asg_r103_validation_v3_ziW9LP/runs/finite_validation/dev16`.

| Task | Old decision count | Corrected decisions | Protocol repairs | Recorded tokens |
| --- | ---: | ---: | ---: | ---: |
| train_2 | 0 | 10 | 0 | 140989 |
| train_3 | 0 | 8 | 0 | 169337 |
| train_15 | 0 | 10 | 0 | 195593 |
| train_16 | 0 | 13 | 0 | 221289 |

All four original successes and 727208 recorded task tokens are preserved.
SHA-256 comparisons of all 250 source-run files before/after passed; no Trace,
capsule, bank or usage file was rewritten. These four Traces have no recorded
DATA_FLOW consumption, so OBS-2 has controlled-test coverage only here, not a
claimed real deployment sample. The interrupted fifth task's raw attempts/usage
remain at the old source; the four-task total does not include that partial work.

## Stop / restart and release boundary

The review recommended keeping v3 running. The user's explicit instruction to
stop and restart takes precedence. Python 210030 was stopped; both it and its
supervisor 210020 exited. Four completed tasks and all interrupted-task files
remain untouched. Restart uses a fresh empty-bank isolated WSL checkout, never
hot-patches or merges changed code into the old run. Only the fixed dev16 runs;
no formal Train120/Test134 or additional paid suite is started.

Prior real T1 evidence is inherited as permitted by the review. Full dev16,
final maintenance/freeze and pending T2/deployment evidence still require later
inspection; passing offline tests and startup are not final experimental release.
