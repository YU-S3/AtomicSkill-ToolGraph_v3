# R10.3 finite real mechanism audit — validation in progress

No formal Train-120/Frozen-134 was started.

First locked execution copy (preserved, stopped on a source-reader bug):
`/home/yangchengyu/asg_r103_validation_0vlGjI`

Corrected fixed copy:
`/home/yangchengyu/asg_r103_validation_v2_n435n2`.
It uses a fresh independent bank and the same predeclared dev order, not the
interrupted bank. A new complete finite validation is required; it is not a
resume under changed code. Both attempts and their costs remain available.

Evidence root: `runs/finite_validation/` beneath that copy. A Git pointer records
the base worktree HEAD; `execution_code_manifest.json` captures the exact patched
file hashes. Base Git SHA alone is not the patched implementation identity.
The first complete copy regression had 1659 passes and a missing-Git-HEAD error
in an existing diagnostic test. Adding the metadata pointer fixed the setup;
the test was not weakened. No API request had run at that point.

Schedule declared before model execution:

1. Full pytest on the fixed copy, then T1 and fixed dev16 in isolated banks.
2. dev16 order: 2,3,15,16,28,34,36,40,45,46,53,57,1,7,12,31,
   resolved from the unchanged train_120 manifest, not constructed task IDs.
3. After successful T1/dev16 completion/freeze, same readonly dev bank,
   C11/C01/C10/A0 on the first **two** declared dev tasks in each condition.
   This small fixed prefix is not selected by success, and is not held-out data.
4. No C00, independent learning ablation or formal benchmark score is claimed.

| Mechanism | Controlled evidence | Real status |
|---|---|---|
| T1 Return → DataFlow → zero-request successor | Existing executor regression | **Passed**, 100.57 s; actual provider counts 4→4 and successor attempts 0; Active fixtures are interface evidence only |
| T2 ordinary multi-step E1 → Builder → source replay | `test_r1021_i.py` | Pending dev16/source inspection; trial is not a substitute |
| T3 repeat extraction → exact reuse | identity/replay regression | Pending; no artificial duplicate E1 requests |
| T4 two trial sources → attributed Active → later deployment | actual controlled runner/admission/freeze | Pending; lack of natural trigger will be reported |
| T5 sidecar two original source replays → later use | two-source positive/negative replay regression | Pending; replay admission alone is not deployment |
| T6 old/new actual presentation comparison | same-surface and intervention tests | Pending fixed two-task C11/C01/C10/A0 slices |

`provider_payloads/` saves actual messages/native tools, never authorization
headers. `provider_attempts/` saves adapter retry audit; `all_usage.json` is
persisted incrementally. The provider probe is stored separately and is not
silently merged into per-task costs. Unknown reasoning remains null. All task
attempts, errors and failed outputs are retained.

The first fixed-copy test process was intentionally stopped **before API
execution** to synchronize the discovered multi-Tool index ordering correction.
No paid task was restarted to improve its generated result. Final status must
be updated from actual logs; pending is not passed.

Fixed-copy pytest: **1660 passed / 104.78 s**. Both provider capability probes
passed. Code hash for T1 and dev16:
`97513ddc4b9303de03d783f3ccfc2838a3345a4ed5298a67a28e18182ca8ad61`.
T1 Trace: `trace_202a2a0ce3e749dc9611545a095d0328`.
The actual live source has not been edited. Subsequent worktree-only changes
are audit documents and extra negative assertions, not execution policy.

## Actual source-reader failure and correction

The first dev run committed task `alfworld_train_2_pick_two_obj_and_place`
successfully: official won and learning eligible, 238897 recorded tokens
including offline learning, 375.27 seconds. Its ordinary E1 produced nine
single-action occurrences; it is **not** T2 multi-step evidence.
Trace: `trace_fceabb1f995341388a7a5ff363224d3b`.

Task 3 then reached historical source retrieval and failed because
`TraceNormalizer` read `item.learnable` on a persisted dictionary. After fixing
that representation boundary, read-only revalidation exposed the independent
batch-allocation issue: a saved occurrence numbered 001 becomes 000 when its
proposal is validated alone. The source verifier now retains the original
indexed allocation ID and compares every remaining source/evidence field.
It rejects a mismatched indexed ID or changed prefix; no validator is bypassed.

All nine preserved real source capsules passed original Normalizer/Atomicizer
revalidation and bank source-integrity checks, with no API or world execution.
Worktree proof: `runs/r103_validation/source_roundtrip_actual.log` and
`tests/test_r103_source_roundtrip.py` (three passing cases).

The interrupted dev run's incremental `all_usage.json` retains **352524**
known tokens across 40 usage entries, including the interrupted task; provider
probes and T1 are separate. Do not replace this with the completed-row sum.
Its `attempt_history`, checkpoint, bank, request captures and log were retained.

After correction: complete pytest **1673 passed / 108.01 s**. Fresh fixed-copy
dev16/T1 and paired diagnostics are pending; formal 120/134 remains unstarted.
