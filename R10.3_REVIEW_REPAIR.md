# R10.3 review follow-up — 2026-09-22

Baseline: `362ffa1af57025a0b9d85779cfd956e542cbb293`, main only.
Review: `R10.3_362ffa1_审查_Compiler小补丁_单Seed三次测试_交付包(1)`.
No baseline branch, historical bank, task selection, prompt, native schema,
validation/admission gate, budget or lifecycle policy was changed.

## Actual dev16 interruption

Preserved failed execution: `/home/yangchengyu/asg_r103_validation_v2_n435n2`.
The first declared task (display 2) committed successfully: 257395 recorded
tokens including learning, 398.862 seconds. Display 3 reached learning evidence
commit and raised `RuntimeError: conflicting learning source identity`.
This was a deterministic source-index idempotency bug, not an API failure.

Ordinary E1 and sidecar E1 independently attested the same original source,
support events and Atomic contract. Their author phase labels, batch-local
occurrence IDs and descriptions differed, hence distinct content-addressed
capsules. The index correctly identifies one independent source, but incorrectly
required its descriptive capsule identity to be identical on every submission.

The correction keeps the first immutable index/capsule, accepts duplicates only
after exact Atomic role-mapping proof and equality of original normalized facts,
prefix/action/effect/authority evidence, and rejects changed parent Trace hashes,
index identities, profiles or evidence. It neither adds support credit nor
replaces historical source text. Both submitted raw capsules remain available.
The two conflicting real sample groups were compared read-only with the fixed
matcher; both are equivalent. Existing raw Traces/checkpoint/usage remain intact.

## Other bounded changes

- Route deduplication now switches the entire representative row, including
  ranking stats and exposure counts. Candidate-first/Active-later regression
  uses different histories and the production compiler.
- Trace-only `r103.compiler-observation.v1` records public reset hash, final
  selected graph, node request/action windows, actual Impl/Tool/native-call
  associations and consumed typed DATA_FLOW references. Final canonical action
  membership uses the existing rollback authority. No new world calls or
  planner/runtime decisions are made to obtain observations.
- Complete automatic programs, partial terminal effects and already-satisfied
  entries are distinct; original R10 counters are unchanged. Source deployment
  uses recorded typed admission provenance; missing provenance is unknown.
- `experiments.compiler_metrics` emits `compiler_task_metrics.jsonl`,
  `compiler_summary.json`, `compiler_metrics_dictionary.md`. Formal report
  integration reuses original resource accounting including auxiliary attempts;
  invocation links never add usage. Older missing capture remains null. The two
  old dev Traces produced an offline report with zero observed / two unknown
  captures and 460119 recorded tokens, not a completed dev score.
- That read-only report check also exposed the old ordinary-E1 count invariant
  being applied to sidecar Builder records. New sidecar records explicitly name
  their validated source audit and record indices. The ordinary conservation
  check stays intact; total Builder counts and all costs still include sidecar.
  Old untagged Traces are not rewritten or guessed into the new format.
- The supplied isolated three-repeat helper and its tests are integrated as
  `experiments.prepare_r103_repeats`. Only the three output paths change; source
  bank, task seed, experiment name, reasoning and original guards stay fixed.
  Do not add new hashed source files to a checkout after formal training starts.

Production metadata names/values remain those in `knowledge.r103_protocol`:
`r103_design_revision=execution-expression-v1.1`,
`runtime_policy_projection_version=r103.expression.v1` and the existing
`expression_upstream_commit`. The observer version is not a new policy identity.
Full historical E1 input cost remains an observation item; no summarizer added.

## Verification and release boundary

Controlled CM01--CM06 cover request/tool/action/validation/normalized-E1-source
invariance, final-route accounting, actual automatic program/rollback action
unions, publication versus consumption/override, metadata roundtrip and cost
conservation. Additional regressions cover duplicate-source corruption rejection,
representative ranking, sidecar report boundaries and the supplied repeat helper.

Final full pytest: **1703 passed in 118.99 seconds**. Local log:
`runs/r103_review_pytest_release.log`. The prior intermediate runs are retained;
the final count is not a claim that the initial patch was error-free.
Real old-Trace offline outputs: `runs/r103_review_legacy_compiler/`.
Fresh dev launch details are recorded below after execution.
Existing real T1 remains valid interface evidence: 54.33 seconds, Trace
`trace_f9c090c622174dacbb1944fe801474e3`, provider counts 4→4, successor attempts 0.
It is not learned-asset promotion evidence and is not repurchased.

A new fixed-checkout dev16 starts with an empty bank and the unchanged declared
order `2,3,15,16,28,34,36,40,45,46,53,57,1,7,12,31`. It does not resume the failed
bank under changed code. Final dev16 completion, ordinary multi-action T2,
maintenance/freeze and pending declared deployment diagnostics remain to inspect.
No formal Train120/Test134 or score/efficiency claim is authorized by this report.
After that inspection, the formal plan is seed42 Train120 → freeze → three fresh
Test134 processes against the same bank, not three independently trained seeds.
