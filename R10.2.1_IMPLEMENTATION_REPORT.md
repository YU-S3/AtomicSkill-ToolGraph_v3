# R10.2.1 实施与验收

## 合同一致性、可选参数与维护容错（2026-09-19）

本节对应《合同一致性_可选参数与维护容错_修复冻结文档》。最终生产代码提交 `4fa2a2343fa582b2fd3284fcf6b5263066d2e598`（主修复 `4fe164d`，源重放编译入口补齐 `4fa2a23`）；隔离执行目录 `/home/yangchengyu/asg_r1021_contract_final_ZPPTJF`，执行代码 hash `7df0e70bbd65d93051bc21324196732bd9978187bc25f3edbe10409dc5eb4378`。下方旧版放行结论不能替代本节。

### 实际改动

1. `tooling/proposal.validate_output_semantic_constraints` 原规则保持，是唯一静态规则来源。删除 E1 `typed_boundary` 的弱化重复循环；Canonical 改写前后、maintenance replacement、Registry 写入前、Builder/static（含 no_tool）、ToolCompiler 两个编译入口和 Runtime 结果认证共同调用它。未向 Atomic 构造器加拒绝规则，旧归档仍可读取。显式 null/list 不冒充空约束；Runtime 原先的 falsey 跳过也已移除。
2. 源重放 `ToolCompiler.compile` 原先遗漏 `output_semantic_constraints`，现先检查并保留作者约束；空约束不改变旧空合同。正常 Builder 编译、显式映射、身份／输出推导和真实 replay 仍保留。该遗漏由代码收尾核查和旧版红测确认，不是因随机 smoke 未达某个成功率而追加设计。
3. 首 Tool preflight 与 ImplementationRunner 共用 `invocation_compiler._tool_arguments`，删除 Runner 重复解析。仅在声明的 SKILL_INPUT 源 optional、目标 signature optional 且键确实不存在时省略目标键；给定 falsey 值原样保留并继续原类型／身份检查。required 判断复用 `entry_contract.required_tool_parameters`，保留原 legacy 标记回退。首 Tool 仍只允许其原有表达式；串行前序输出不存在仍失败，错误进入原 ValidationRecord。
4. `submit_typed_repair` 原字段不变，replacement schema 复用既有 ParameterSpec／BindingExpression／SemanticPredicate 结构并明确 ref。Atomic／Implementation 的必读字段、嵌套对象和枚举在不可信解析边界检查，直接 parse/build/engine 入口亦覆盖；不补 ref、不用字符串化拯救坏值。target_refs 仍由代码授权。System 原内容错误拒绝 batch、内部 KeyError/RuntimeError 传播、费用与 finalize 路径均未改成全局吞错。
5. 在原共享 prompt/native resolution description 说明兼容约束要求；不自动改 required_resolution、不删除作者约束。没有新增公共模型字段、工具名、Agent、预算、retry 池、运行模式或数据库表；不加 benchmark 特判、不恢复自动 Support。600K/100 在线共享预算、262144 离线池、Candidate/Active、Repeat、真实 witness、源证书、backend 复用和 Frozen 边界不变。

### 工程验证与反例

全量 pytest：本地 WSL **1541 passed / 54.07s**；最终锁定 Linux checkout **1541 passed / 45.13s**。`git diff --check` 通过。三个旧测试桩仅补全真实消费者所需的 inputs/signature 声明，原 Repeat、trial 隔离与 witness 断言未删除。

| 组 | 实际生产路径证据 |
|---|---|
| C1 | `test_r1021_contract_consistency` 与 `same_contract_consumers.json`：同一序列化合同经过 E1、Canonical、repair、Registry、R0 输出合同检查、Builder/no_tool、Runtime；semantic 无附加约束及 concrete 合法约束通过，semantic+约束、optional 锚点、未知角色、null/list 拒绝。R0 无 author bindings 的矩阵只声称输出合同检查通过，不冒称完整 R0 PASS。源重放编译另有保留／拒绝测试。 |
| C2 | `test_r1021_learning_responsibility`：受控 native E1 合法能力＋坏兄弟→Canonical→实际 Builder→static→源 replay→准入／持久化→E2→重新读取→真实 Runtime/AtomicValidator/publisher→下游 DataFlow；semantic identity、concrete 两个版本均通过。Candidate 未伪造 Active，验证器未替换为 PASS。 |
| C3 | 同一 Implementation 的完整 preflight/Runner，源 optional→目标 optional 缺省／给值、目标 required 缺省拒绝／给值执行；registered 与 runtime_trial 均覆盖。0、False、空字符串、空列表、None 不被当作 absent。 |
| C4 | 全量保留自动节点、当前显式工具、helper、Runtime trial、事务与串行消费者测试；`optional_failures.json` 实际执行验证作者必经 ACTION 读取缺失值仍失败，前序 Tool 真成功但后续引用不存在仍 mapping_error，逻辑状态回滚且动作仍计费。 |
| C5 | `test_typed_repair_session`、`test_typed_repairs`、新定向参数化：缺 ref、非法 skill_output、错误嵌套类型、合法 Atomic/Implementation/no_change；新 schema 真实 API 接受，返回合法 no_change。 |
| C6 | 测试通过真实 `run_v3_train.run` 的任务循环、`system.run_task`、周期维护、AttemptTraceLedger、ManifestStore 和 checkpoint：环境真实 won→坏提案拒绝→runner completed→checkpoint 清理；注入内部 KeyError 则 infrastructure_failed，checkpoint 保留。只固定测试场景、模型返回及单题选取，不手工登记 completed。原始缺 ref payload 又经同一链重放，`original_maintenance_replay.json` 通过，无付费模型调用。 |
| C7 | 原 `trace_680db4e73f5640a4ba24eac5bf5a06cf` 缺 ref payload 原样拒绝；旧 bank 实际 6 份冲突合同拒绝。旧 `9df9921` 执行新增核心反例：14 failed / 9 passed，包含真实 E1 接受差异、optional 调用前误拒和 KeyError，不是缺新接口；源重放遗漏红测另 1 failed。旧合同不回写、不改分。 |
| C8 | 全量预算、identity/关系证据、terminal、Repeat、maintenance、源身份／证书、环境 backend 复用、Frozen 和资产断言保持。原 120 train／134 test 配置未修改。 |

原现场 `/home/yangchengyu/asg_r1021_seed42_Z3x3ku` 保留；两次只读清单的 392 个代码、配置、日志及运行证据文件 hash 相同。不登记第 18 题、不清 claim/checkpoint、不迁移 bank、不使用 provider-recovery。

### 有限真实验收与版本边界

主修复 `4fe164d` 的一次两题 smoke：2/2 won，586.10 秒，296880 tokens；train2 的两项 E1 来源证据不足正常拒绝，train3 的四项 E1/Builder 接受；E2 有一次原协议 repair。末批维护、资产、费用及诊断 Frozen 审计通过。此轮在源重放入口补齐前，**不冒称最终字节的实验结果**，全部证据保留于 `runs/r1021_contract_validation/validated_4fe164d/`。

最终提交的真实两节点检查通过，27.22 秒：明确声明的 Active fixture/program（不是自主学习）经真实 ALFWorld、ToolRunner、AtomicValidator、typed publisher 与 DataFlow，下游省略 optional hint 正常自动执行；两节点 Runtime provider 计数 `[1,1]`。同组真实 API 接受完整维护 schema 并返回 no_change；合计 2 次调用、9090 tokens，provider probe 另计。该 fixture 不被宣称为 E1 自主发现或正式 task 成功率。

最终代码两题 smoke **2/2 won，543.41 秒，344366 tokens**（train2 144349、train3 194991、末批 maintenance 5026；独立 provider probe 另计）。两题均为 full_dynamic，不冒称自然图复用。train2 的 E1 3 项中 2 项有效、1 项拒绝，合法兄弟保留，Composite 为 terminal_empirical，未声称 task contract 全覆盖；train3 的 E1 9/9 有效且 task contract 覆盖通过。Builder 有 1 次原协议 repair，没有增加重试池。

末批维护正常返回、费用与资产审计 `audit_errors=[]`。最终资产为 4 Atomic、2 Composite、6 Implementation、6 Tool，全部 Candidate，未伪造 Active。诊断 Frozen 检查通过，readonly=true，源 digest `7a80c4dde18d63946ce206138352d981dae6ad51f513ffd920c9fa39cf606a9e` 前后不变；没有运行正式 eval。原配置预检和同 Provider probe 通过；provider fingerprint `9ae66a5844fdc307babbfa37be88d819c3ac26f7e0953da6285c237f1b9c7ed3`。120 train 清单 hash 保持 `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640`。134 test 预检使用独立空 fixture snapshot，只证明配置与冻结执行入口可用，不代替正式训练来源验收。

最终完整验收证据已平行保存至 `runs/r1021_contract_validation/validated_4fa2a23/`：`pytest.log`、`dataflow_optional/summary.json`、`smoke/`（原 Trace、bank、汇总）、`smoke_audit.json`、`smoke_funnel.json`、`smoke_frozen_readonly_audit.json`，以及同合同消费者、原始坏提案重放与 optional 失败检查。两轮验证均保留，不合并成一轮或挑选较低费用作为最终结果。

结论：本轮冻结范围的工程验收通过，可以启动新空库 seed42 120 train→134 frozen test；小样本 smoke 不承诺正式成功率或自然复用率。旧 `asg_r1021_seed42_Z3x3ku` 不 resume、不迁移。正式实验尚未启动；应从 main 创建新的 Linux 本地 checkout，依次运行 `experiments.run_v3_train --config configs/alfworld_train_full_120_r1021_seed42.yaml` 和 `experiments.run_v3_frozen_eval --config configs/alfworld_frozen_eval_134_r1021_seed42.yaml`，使用 `set -euo pipefail` 确保 train 非零退出时不会启动 test。

诊断附件保留驱动调试失败：同合同矩阵最初用了不符合原 R0 输出 derivation 的 fixture，修正为诚实的 effect_witness fixture 后通过；原 payload 重放驱动曾用相对路径导致 fixture Trace 目录不匹配，修正为绝对路径后真实 runner 收尾通过。这些均没有修改生产验证器或正式结果，也没有付费重试。

## 学习闭环与重放开销收敛（2026-09-19）

代码提交 `35e4a3211f6d5686f1f55462eec436da150675d0`，隔离执行目录 `/home/yangchengyu/asg_r1021_learning_cbFRMf`，执行代码 hash `a57e9a8633dc2b75cca2314fd6a98225b84d29d02102e0cd807391de78cc493e`。
本节依据《学习闭环与重放开销收敛修复规范》；下面 09-18 的放行及方法结果属于旧版历史，不能代替本轮结果。

### 工程修改与断言

1. 在现有 E1 输入段和 native ParameterSpec 字段 description 说明未来调用责任。原默认 false、schema required 集合、源入口证据与 Builder 合同一致性保持。代码**没有**把 false 改 true；解析器与 canonicalizer 原传递正确，本轮没有重写它们。
2. Adapter 缓存只读定位元数据（真实 discovery reset 观察的 index/file 不可变 tuple），请求的路径不能自行认证。配置/根目录/split/过滤身份变化失效；身份不符拒绝。未发现 index 沿原发现路径核对一次；明确 legacy/不支持接口仍走原合法路径。
3. 已核验文件通过窄私有子类覆盖官方 collect_game_files；原构造器、init_env、Expert/Demangler/Infos、seed、100 步限制全部保留。同文件重用 backend **不重用世界状态**；每次真实 reset、完整 prefix、原 ToolRunner 和结果认证仍执行。再次 load_tasks/load_balanced_tasks 回到完整发现路径。
4. backend 替换/退出复用幂等清理。System 只关闭自己创建的 Adapter，不关闭调用者注入的 Harness。没有新增环境池、数据库表、证据服务、模型字段、运行模式或 replay authority 版本。
5. 原 ReplayCertificates 的来源解析顺序与完整键查找正确，保留不改；没有将同 Tool 的不同 case 合并。CompositeBuilder 原 runtime 缺口规则正确，false 无来源仍拒绝，不添加 E1R 或预求解器。

| 验收组 | 实际断言与已有边界 |
|---|---|
| L1 | `test_r1021_learning_responsibility`：native E1、typed Atomicizer、序列化；false 默认与非必填保持；同 intent 的 true/false 合同分开持久化 |
| L2 | 同构造中 true runtime 缺口、false 无来源拒绝、false Task/DataFlow 正例；已复用上游输出缺显式边仍拒绝 |
| L3 | 受控合法 E1 加坏兄弟→真实 System/Atomicizer→Builder/静态/源 replay/准入→真实 E2→Composite 落库读取→缺参 Agent 调用→typed 返回/DataFlow→下节点；Candidate 未伪造 Active |
| L4 | true 的未来源证据仍拒绝；原 identity/Repeat/scope/缺参/concrete、无自动 Support 测试全部保留 |
| R1 | 真实旧/新四 case：同任务两 prefix、env_index=30、跨任务、原拒绝；file/goal/id/初始观察/catalog/语义快照/prefix 后状态/实际动作/RETURN 对照 |
| R2 | 同文件三次 reset 只建一次精确 backend；跨文件替换、异常后重建、配置失效、切回全发现；创建/关闭计数一致 |
| R3 | 真实准入后的 pending 与发布后完整键 PASS 复用，不执行 reset；改程序/case 内容/authority 不命中；旧四 case 的必要执行不缩水 |
| R4 | 原全量：预算、源身份、账本、事务、maintenance、publication、Frozen、won 等断言未放宽 |

全量回归：本地 WSL **1509 passed / 55.31s**；锁定 Linux checkout **1509 passed / 41.94s**。`git diff --check` 通过，实际 import 来自锁定目录。
测试中的预声明生成与场景不是模型自主表现，也未把验证器替换为 PASS。没有按测试数量删有效检查。

### 同一组 case 的物理开销

源为暂停的 c044988 正式运行一致备份；执行前固定 `physical_comparison/declared_cases.json`，旧/新各跑一次同程序同 case，无模型调用。新旧使用隔离诊断 bank，不改旧 bank/Trace/提案。

| 同 case（global env_index） | prefix 动作 | Tool 实际动作 | 旧/新结果 | 旧定位与 case reset | 新定位与 case reset | 旧/新秒 |
|---|---:|---:|---|---:|---:|---:|
| train_0 / case 3749a20c | 1 | 1 | PASS / PASS | 1 | 1 | 4.359 / 0.415 |
| train_0 / case 5fb41c72 | 5 | 1 | PASS / PASS | 1 | 1 | 4.388 / 0.532 |
| train_30 / case 3d73370a | 0 | 1 | PASS / PASS | 31 | 1 | 38.699 / 0.873 |
| train_13 / case 8e3f73da | 29 | 0 | 原 entry_constraint 拒绝 / 同样拒绝 | 14 | 1 | 24.092 / 4.539 |

- 不含初始发现：目录收集 **4→0**，backend 创建 **4→3**，底层 reset **47→4**；四次必要 case reset、35 个 prefix 动作、3 个 Tool 动作全部保留。
- 含双方各一次前31场景发现：目录收集 **5→1**，backend 创建 **5→4**，底层 reset **78→35**。
- 四 case 回放合计 **71.54→6.36 秒**；含发现总耗时 **107.47→46.18 秒**。这只是固定小样本的物理准备测量，不是 token 节省或正式整轮加速保证。机器同时有用户两个消融实验，本轮未停止或修改它们。
- 实际 RETURN 比较字段为 `ToolExecution.output_candidates`；不是比较不存在的 `outputs` 字段。最初诊断脚本该项取错字段，在已保存完整执行结果上纠正复核，四例真实值一致；未重抽 case 或重跑以改变结果。
- 必需 fresh replay 数仍为4，不把每个不同 case 说成重复证书。原拒绝 case 没有改成 PASS。

### 旧正式运行与本轮方法检查

用户暂停的旧 run 实际完成 **34 题、30 won**，不是文档引用的30题截面；本轮不将它改名为成功 pilot。已确认原进程退出，完整复制 runs、configs、train.log，SQLite integrity_check=ok。保留原 running/claim/证书及失败费用；没有 resume 或回写 false。

Windows 备份：`D:/T3S_exp/AtomicSkill-ToolGraph_v3/runs/r1021_learning_replay_validation/old_seed42_interrupted/`。
物理对照及测试日志：同级 `physical_comparison/`、`pytest_final.log`、`targeted.log`；只读生成责任审计 `old_responsibility.json`，最终接受提交和 raw 分母分列。

旧记录复核：前30个完成任务最终 E1 提交235个 input specs、raw 含协议修复前244个，均false；实际34题最终254个、raw263个，均false。这里确认的是生成选择，**不是**解析器丢值或 CompositeBuilder 应放宽拒绝。

有限真实模型使用锁定 checkout 的新空库、原 `--mode smoke` 固定 train2/train3 各一次。**2/2 won，393.53 秒（6分34秒），代码 hash 全程不变，34次模型调用、331,627 tokens（含5,451 maintenance；不含独立probe的4次/1,778 tokens）**。

| 实测 | train2 | train3 |
|---|---|---|
| 路线 | full_dynamic | stored_composite（Agent 显式调用，自动图节点0） |
| task tokens | 204,405 | 121,771 |
| task 秒 | 250.15 | 128.54 |
| 最终 E1 提出/合法 | 5/5 | 1/1 |
| 最终 input specs / runtime=true | 9/9 | 1/1 |
| raw input specs（含修复前） | 18，均true | 1，true |
| E2 | 首次通过、完整覆盖 | 首次通过、原 terminal_empirical 权威（不冒称完整TaskContract覆盖） |
| Composite | 真实入库 | 真实入库 |

全体最终 E1 为6 occurrences/10 input specs；raw 为11 occurrences/19 input specs，不能混用。E1 使用原额度内一次协议修复（3调用），E2 共2调用且无E2R；Builder6次 create均过静态，5个Tool Candidate、1个Shadow（`gc_put_affordance`源入口拒绝，保留作者PUT程序和原约束，不做动作替换）。最终4 Candidate Atomic、2 Candidate Composite、5 Candidate/1 Shadow Implementation，无伪造Active。正常拒绝完整计费和保留。

费用分桶：planner_p1 6,620；cold_start_c1 8,604；cold_start_c1_repair 17,593；runtime_dynamic 29,062；runtime_preparation 64,858；extractor_e1 116,006；tool_builder_evolution 75,632；extractor_e2 7,801；evolution_repair 5,451。reasoning包含在completion中，不重复相加。

最终只读审计 `audit_errors=[]`，所有34个模型用量事件归属不可变Trace；实际 smoke 冻结库摘要 `8e91dc1f4548f94c2187d995c739d320c0a97966a914b9f638d4c57c38dfab38`，只读加载前后不变。没有用空快照检查代替实际产物检查。

两正式配置预检均 exit=0：Train120 六类各20，manifest hash `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640` 与旧版一致；Frozen134 134题、failure-side零读取、摘要不变（该配置预检使用显式空快照fixture，不是正式测试成绩）。provider fingerprint `9ae66a5844fdc307babbfa37be88d819c3ac26f7e0953da6285c237f1b9c7ed3`；smoke config hash `9d1e1237f742f1ab33529483ab71e7abb8ea58563599d98bd6d731717cb7ad27`；隔离Frozen预检config hash `e73e9956d4b8ce9fb74e50418ddc56d0ba3eba38a40d5b2c1ea172c471b1f193`。模型、reasoning、输出上限、预算和生命周期数值未改。

运行器/两预检/pytest 全部 exit=0。附属只读审计首次因相对路径转SQLite URI报错；修正诊断脚本路径resolve并在相同已完成产物上重做，最终审计通过；不是实验重跑，没有额外API调用或生产代码改动。失败诊断日志仍保留。

完整结果已复制到 Windows：`D:/T3S_exp/AtomicSkill-ToolGraph_v3/runs/r1021_learning_replay_validation/validated_35e4a32/`。包含 smoke 原始 Trace、bank/frozen_bank、manifest、provider probe、all_usage、最终审计与预检日志。没有复制密钥。

**放行结论：工程闭环、物理等价与有限真实模型验收均完成，可从本提交、新checkout、新输出、empty bank启动Full-120→Frozen-134。尚未证明95%或普遍省token，也不把两版smoke费用不同当prompt-only因果实验。全部诊断已结束，没有启动正式train/test。旧34题不能以新代码resume。**

### 新空库 seed42 启动（WSL）

不需要git pull；本地main已是本轮代码。以下克隆只带已提交代码、不带旧runs/bank；train及其maintenance/audit/freeze成功退出后才顺序启动配对test。

```bash
(
set -euo pipefail
REPO=/mnt/d/T3S_exp/AtomicSkill-ToolGraph_v3
RUN=$(mktemp -d /home/yangchengyu/asg_r1021_seed42_XXXXXX)
git clone --quiet --no-hardlinks --single-branch --branch main "$REPO" "$RUN"
cd "$RUN"
set -a
. "$REPO/.env"
set +a
: "${MODEL_API_KEY:?请先在.env填写MODEL_API_KEY}"
export PYTHONPATH="$PWD/src:$PWD" PYTHONUNBUFFERED=1
PY=/home/yangchengyu/asg_alfworld_venv/bin/python
echo "本轮实验目录：$RUN"
"$PY" -m experiments.run_v3_train --config configs/alfworld_train_full_120_r1021_seed42.yaml 2>&1 | tee train.log
"$PY" -m experiments.run_v3_frozen_eval --config configs/alfworld_frozen_eval_134_r1021_seed42.yaml 2>&1 | tee test.log
)
```

正式输出分别在新目录的 `runs/alfworld_train_full_120_r1021_seed42` 和 `runs/alfworld_frozen_eval_134_r1021_seed42`。Frozen配置仍指向本轮train的 `frozen/data_v3`，不是诊断snapshot。首次不加resume，不搬走或删除旧实验。

## 历史收敛修复（2026-09-18，新规范覆盖旧随机成功门槛）

**结论：工程与协议验收完成，可以从本提交、新空库启动正式Full-120→Frozen-134。自主生成、成功率、效率和多步复用的未证实部分留给正式实验测量，不作为新的启动门槛。没有启动正式实验。**

当前代码提交：`0f3f890c82a6cc28b5a1b982efb3d040313e51fe`；固定执行目录 `/home/yangchengyu/asg_r1021_converged_w5BtYO`。
执行代码 hash：`298ec9729a769e91403a43e0dbf0853e8ff87a898ff77b46991bf44d73b805a8`。
本文后续旧版本的“不满足随机T1/T2所以暂不放行”结论是历史记录，不再是当前启动门槛。
采用《最终收敛修复与正式实验启动规范》：工程正确性、完整收尾与方法效果分开；不要求95%、全胜或自然发现多步Tool。
当前全量回归、两条真实DataFlow、两题smoke及最终收尾全部完成，详见本节后续验收记录。

### 已确认问题、修复与原始证据

| 实际问题 | 修复位置与范围 | 验证 |
|---|---|---|
| 普通E1把没有typed时点的action_argument/semantic_alias列为入口候选 | `System._prepare_evolution` 用原 `public_value_authorities(trace)` 替换旧目录，删除重复action_arg注入；原合格Runtime trial输入仍加入同一目录。E1与Atomicizer读取同一对象 | 新生产Trace→System实际请求→合法提案→Builder/static/replay/admission正例，非法兄弟独立拒绝 |
| typed正式校验仍可进入旧动作/alias授信支路 | `_input_authorities` 对当前typed路径不接受这两类；明确legacy_source_replay隔离路径仍保留，不改归档原文 | 当前typed旧来源负例；原历史/失败回放与身份、local、scope测试保留 |
| 时点错误统一写成“入口不可用”，容易误读为未来值 | `typed_boundary` 原消息包含ref、entry_revision、实际available_revision，区分缺失typed时点与晚于入口；scope/type/resolution继续分层拒绝。投影不再把bool/string revision强转int | 原typed回归与真实旧请求逐条核对 |
| Builder允许未实现callback，Harness错误声明支持 | 原ToolProposal schema只允许三种内建constraint，verifier_id仅省略/空；`AlfWorldAdapter.supports_constraint`删除虚假声明；原static复用它，`check_tool_entry`同样检查 | 三个真实支持正例＋current_context/custom_adapter/伪造非空ID负例贯通schema/static/Runtime |
| 旧train45 FailureExtractor已发现prefix无效，仍索引缺失by_step导致KeyError | `FailurePlanAlignmentValidator.validate`只在prefix_valid时读取prefix_end；非法前缀沿原`failure_extractor_alignment_invalid`退出，不伪造对齐 | 缺step／缺witness／越界三类回归；旧原始F1提交零模型调用复验不再异常 |

没有新增模型字段、工具名、数据库表、协议版本、Agent、callback、预算、retry或Runtime路线。
未改baseline、官方成功定义、对象/任务规则、参数授信、执行器、terminal、Candidate阈值和Frozen权限。
两段prompt是在原位置替换；I的源入口/typed样本/正负delta仍只提供一次。

旧dev16已退出，不做hash混跑；原目录未修改。完整中断现场备份在
`runs/r1021_final_validation/validated_699bacc/dev16_interrupted/`，日志为同级`dev16_interrupted.log`。
只读复核在 `convergence_old_source_audit.json` 与 `convergence_old_f1_recheck.json`：
旧完成8题均won，55个E1候选中5合法、50拒绝；train34的5条Builder记录为created（不因此宣称Active/部署）。
实际旧提交共69个输入引用缺少typed时点，不能继续全部称为“模型用了未来值”。
各旧task仍有现存公开typed记录可重投影；旧train6有128条，故不声称其完全没有来源，也不再次付费重抽它。
新旧目录差异可由实际请求、原ref及revision逐条审计，不回填旧Trace。
旧train45原提交的再验证返回`matched_prefix_authoritative=false`、`failure_extractor_alignment_invalid`，源SHA/字节未变、模型调用0。

### 八组工程回归

Windows **1501 passed / 30.27s**；锁定Linux checkout **1501 passed / 34.65s**；`git diff --check`通过。
`results/import_identity.log`确认实际导入锁定目录src与experiments；配置仍是原R10.2.1的120/134两份。

| 组 | 实际断言位置（tests/） |
|---|---|
| A 目录/生产者 | `test_r1021_i::test_current_system_typed_directory_to_builder_replay_and_admission`：真实native Trace、public_binding/public_catalog同时可见、实际请求只有同一目录；`test_r1021_h`实际请求新formal角色 |
| B 时点/local/scope | 原`test_r1021_boundaries`、`test_r1021_final`、`test_r1021_i`：未来/跨owner/回滚/稀疏index/使用前local，新增当前typed旧动作来源负例 |
| C 学习全链 | 上述新生产全链正例：真实Atomicizer、Builder、static、source replay及实际注册均未设为总通过；原retention/no_tool/坏兄弟保留 |
| D 支持一致 | `test_r102_contract_boundaries::test_formal_constraint_schema_static_and_runtime_agree`六例；原自定义回调隔离测试仍有效 |
| E DataFlow/Agent | 原`test_r102_execution`与显式helper/ownership回归；真实两节点验收见下表，fixture程序仅在诊断driver，不入生产策略 |
| F 失败/terminal | `test_failure_extractor`新增无效prefix三例；原事务、费用不退款、RETURN/未执行ACTION、official won回归 |
| G Frozen/账本 | 原formal guards、orphan证书、typed发布、600K/100/262144、Candidate资格、Frozen零写入测试全量保留 |
| H 请求接线 | `test_r1021_h`实际native E1/Builder/E2/Dynamic请求，I真实System请求；本次真实Builder服务接受收窄schema，两次create、协议repair=0 |

旧“接受action_argument作当前入口”的断言迁移为公开typed正例＋旧动作负例。
隔离E2/预算/资产保留fixture用已有grounding_evidence_changes格式声明原本预先给定的值；这不是生产推断器，也不用于新全链测试。
没有删除失败测试或原有效边界来换取全绿；此前已经删除的三个重复substring测试不恢复，本轮不按数量删测试。

### 有限真实验收（不运行第二轮完整dev16）

| 检查 | 结果 | 时间／模型tokens |
|---|---|---|
| 预声明程序＋真实ALFWorld两节点DataFlow | 通过；两节点真实动作、typed返回、下游自动，调用计数[1,1]；无新增下游Agent调用 | 55.569s／10,833 |
| 真实Builder生成＋同一DataFlow驱动 | 通过；两个create均schema/static通过，下游自动，调用计数[4,4] | 64.203s／29,293，其中Builder25,769 |

这两条都是声明合同/Active状态的集成fixture，不是自主学习成功率。
预声明程序模式仍保留原上游bootstrap Agent，因此不是“全链零LLM”。两条独立费用/Trace审计`audit_errors=[]`。
真实生成检查保留所有原始请求/工具提交；未实现的current_context原坏提交仍在旧目录，没有转写或覆盖。

固定两题smoke使用现有`experiments.run_v3_r102_targeted --mode smoke`，预先声明train2、train3，新空bank；
这只是原诊断driver任务子集，不新增Runtime接口，不替换任务，不以任务成功作为收尾条件。
诊断驱动仍按原 `finalize_diagnostic` 执行maintenance、artifact审计与freeze；不与正式bank混用。

### 两题最终结果、计量与放行依据

固定两题均完成并won，退出码0，总历时 **832.097秒（13分52秒）**，共 **42次调用／527,153 tokens**。
其中train2为190,769，train3为328,422，末批maintenance为7,962；reasoning属于completion，不重复加算。
本smoke独立provider probe为4次调用／1,755 tokens，单列，不混入上述任务/maintenance总量。

| 任务 | 路线 | E1有效提交候选／合法／拒绝 | 学习结果 |
|---|---|---:|---|
| train2 | full_dynamic | 8／6／2 | 合法Atomic继续，E2形成Composite；两项谓词包含未绑定具体实体，正常拒绝 |
| train3 | stored_composite | 8／8／0 | 部分Atomic/Tool保留；E2实际调用触发原离线共享预算耗尽，没有新Composite，任务仍won |

E1共3次真实调用（含1次原额度协议修复），最终2个schema接受的提交共16候选，14合法、2拒绝。
原raw漏斗`submitted_occurrences=24`包含协议修复前提交，不能当独立有效候选分母。
Builder共14个session／15次调用（1次原额度协议修复），14个最终schema接受提交，全部create，no_tool=0；
13项static通过、1项因`ACTION n1_take must declare expected_effects`拒绝。不同session结果可能规范化复用同一资产，不能把请求数当资产数。
E1费用179,105、Builder187,382、E2 19,437；其他在线与maintenance分桶见`smoke_audit.json`。

最终普通学习Tool资产12个：**10个源replay通过、2个Shadow**；对应Implementation也是10 Candidate＋2 Shadow。
另有7个Candidate Atomic、1个Candidate Composite；无伪Active晋升，无普通E1多步Tool准入证据。
第二题确实选择stored_composite，但自动图节点计数为0，不能宣称该题已证明零LLM自动复用或token降低。
两项Shadow的实际作者条件为`harness_affordance / action_type=PUT / argument_mapping={}`，
而对应源动作类型为`MOVE`；原入口检查返回`tool_entry_constraint_unsatisfied: gc_put_affordance`。
这属于作者声明不适用条件的正常拒绝：未改成MOVE、未删条件、未跳过replay，不额外重抽。
第二题离线预算失败仍完成任务审计、合法兄弟保留与maintenance，不新增预算池或重试。

最终maintenance无pending proposals，artifact/identity/usage审计 **`audit_errors=[]`**；42个usage event均能与不可变Trace对账。
真实诊断产物`smoke/frozen_bank`已再次以Frozen/readonly模式打开审计，无provider调用、digest不变：
`3346a8f0e81df3d7ebd7b8a05c201f13b68e146164c87f6991d5dc627f2c9875`。
Train120/Frozen134配置预检均通过；正式task manifest仍为六类各20／OOD134，未改任务或模型。
Provider fingerprint为 `9ae66a5844fdc307babbfa37be88d819c3ac26f7e0953da6285c237f1b9c7ed3`；
smoke配置hash `98cdbed9a152c77883dfa820ff39841116a5e1e3c786058a0a13a1e8f7f74438`，其路径隔离与正式配置差异均保留于config/manifest。

实际付费普通Builder请求已配对保存于 `results/actual_ordinary_builder_source_pair.json`：
源入口before事实、逐事件正负变化、typed输入、canonical prefix均与对应真实源replay case一致，replay通过。
这是新smoke的普通E1来源，不是fixture、旧源重抽或Runtime automation。原native提交、usage、工具status和源SHA一并保存。

Windows完整验收备份：`runs/r1021_final_validation/validated_0f3f890/`，其中`results/`包含：

- `smoke/summary.json`、`smoke/trace_store/traces/`、`smoke/bank/`、`smoke/frozen_bank/`；
- `smoke_audit.json`、`smoke_funnel.json`、`smoke_frozen_readonly_audit.json`；
- 两条DataFlow原始请求/Trace/提交、独立审计、两正式配置预检及执行身份；
- `actual_ordinary_builder_source_pair.json`、全量pytest日志；旧源/F1零调用审计在备份根目录。

### 新空库正式启动（WSL，完成train才顺序进入test）

下面创建新的Linux checkout，不移动、不删除旧runs，不需要git pull，也不复制诊断bank。
使用本地已同步的main，固定代码后不要在运行期间更新该checkout。既有Python环境无需再次安装。

```bash
(
set -euo pipefail
REPO=/mnt/d/T3S_exp/AtomicSkill-ToolGraph_v3
RUN=$(mktemp -d /home/yangchengyu/asg_r1021_seed42_XXXXXX)
git clone --quiet --no-hardlinks --single-branch --branch main "$REPO" "$RUN"
cd "$RUN"
set -a
. "$REPO/.env"
set +a
: "${MODEL_API_KEY:?请先在.env填写MODEL_API_KEY}"
export PYTHONPATH="$PWD/src:$PWD" PYTHONUNBUFFERED=1
PY=/home/yangchengyu/asg_alfworld_venv/bin/python
echo "本轮实验目录：$RUN"
"$PY" -m experiments.run_v3_train --config configs/alfworld_train_full_120_r1021_seed42.yaml 2>&1 | tee train.log
"$PY" -m experiments.run_v3_frozen_eval --config configs/alfworld_frozen_eval_134_r1021_seed42.yaml 2>&1 | tee test.log
)
```

第一次运行不加resume；若train异常退出，`pipefail`阻止启动test。Frozen配置引用同一新目录下本轮正式训练snapshot；不是smoke的freeze产物。
恢复仍按原completed-task/provider/hash协议处理，不能换代码后直接拼接旧结果。

---

## 以下为历次实施与验收历史

基线：`024957adfc368a2d4624b475046d5a39fedcc29c`。代码修复、全量工程回归、固定真实开发集及证据审计已完成；T1/T3/T4/T5 已通过目标路径，**T2 普通 E1 多步能力的真实准入仍未通过，暂不宣告完整放行**。本文件区分工程正确性、路径覆盖与方法效果。未改 baseline、旧 Trace/bank/证书，不接续旧运行前缀。

## 实施范围

| 包 | 生产修改 |
|---|---|
| A | `runtime/budget.py` 仅保留 task 100 个环境动作；`system.py`、`agents/session.py` 从同一 UsageLedger 计算六类在线桶共享 600K，在线 Builder 同池，离线独立；协议轮数错误不冒充 token 耗尽。跨旧阈值仅在 `runtime/r1021_metrics.py` 旁观记录。 |
| B | native E1 boundary v2 明确 input/output ParameterSpec、formal→来源角色、local refs、output constraints；`typed_boundary.py` 核验可用时点、Trace/occurrence 归属、类型及实际认证级别。canonical/signature/Builder 保留合同。普通入口拒绝 legacy；仅失败来源重放显式使用 legacy，当前晋升迁移为 v2。 |
| C | `ExtractionPolicy` 按 canonical accepted events 减去实际完成 Tool 的覆盖及已抽取事件，避免最后一个工具遮蔽准备工作；同 task 一次成功 E1，空 proposals 合法；已验证独立 Atomic 不因 E2 拒绝丢弃。 |
| D | `ToolRunner` 在原 PC/局部作用域内执行 terminal 后纯控制与 RETURN，遇下一 ACTION 停止，不跳过；`ImplementationRunner` 不能执行下一 Tool。当前 partial 与未来 skip 分开；`CreditAssigner` 使用真实图所有权及终止动作归属计算部署贡献，Tool partial 不获完整成功。 |
| E | Tool 全部 final effects 与串行 Implementation 完成共同构成成功闸门；三个结果类型携带实际 typed bindings；publishers 先整批验证再写入，无 raw fallback/整体权限升级；ARGUMENT_EXISTS 逐 JSON 类型和值验据。 |
| F | `task_runtime.py` 是既有 Dynamic 路径的 task-scope 接线，共用 InvocationCompiler、ToolRunner、事务、R0/Builder/static/trial/R1；无假父 Atomic、无额外额度，trial 不重复执行。node/task 晋升按各自实际完成条件、原两来源与 cross replay 阈值执行，Frozen 不持久化。 |

新配置：`configs/alfworld_train_full_120_r1021_seed42.yaml`、`configs/alfworld_frozen_eval_134_r1021_seed42.yaml`。模型/reasoning/output cap 不变；replay authority 唯一来源更新为 `tool_replay_r1021_v1`。

## 生产回归覆盖索引

以下 ID 对应冻结规范的语义场景，不以 pytest 总数代替断言。`tests/` 为下表默认目录。

| 规范 ID | 主要测试与断言来源 |
|---|---|
| A01/A03/A04/A06 | `test_r1021_boundaries.py::test_A01_A03_A04_all_online_buckets_share_one_task_pool`；六桶真实 usage、越界保留、下一 task 重置、离线不侵占；`test_r101_boundaries.py` 资源异常回滚不退款。 |
| A02 | `test_r1021_boundaries.py::test_A02_only_task_action_limit`，同节点 100 动作，第 101 拒绝。 |
| A05 | `test_deepseek_protocol.py`、`test_planner_phase_budget.py` 费用/重复 event/跨 task，reasoning 不重复计费。 |
| A07 | `test_r101_formal_boundaries.py`、`test_formal_guards.py`、`test_runtime_deepseek_repairs.py` 新配置合并、协议及资源错误。 |
| B01/B02/B03 | `test_r1021_boundaries.py` semantic 输入、内部公开实体、显式改名映射、错误来源/未来/local/owner 反例；`test_v32_e1_input_authority.py`。 |
| B04 | `test_v32_r3_semantic_snapshot_authority.py`、`test_v32_r7_semantic_alias_authority.py` entry 前置证据，不从后置状态补入口。 |
| B05 | `test_r9_output_identity_lineage.py`、`test_r921_witness_assignment.py`、`test_r1021_boundaries.py` 实际认证级别与联合输出 witness。 |
| B06 | `test_r92_review_loop_scope.py`、`test_r92_review_output_candidates.py`、`test_r1021_boundaries.py` 真解释器多步/循环/catalog 与失败关闭。 |
| B07 | `test_real_extractor_regressions.py`、`test_r102_execution.py` 实际 E2 数据边与上游返回驱动下游；T1 为对应真实验收。 |
| C01/C02/C03 | `test_r1021_boundaries.py` event 级覆盖、空提案、同一决策去重；`test_evolution_guards.py`、`test_deterministic_fullchain.py` 系统链路。 |
| C04 | `test_extractor_contract_authority.py`、`test_v32_r4_learning_retention.py`、`test_v32_r61_extractor_e2_repair.py` partial/empty/E2 拒绝下保留合法资产。 |
| C05 | `test_r10_runtime.py` sparse canonical/rollback；`test_provisional_promotion.py` 失败源隔离。 |
| C06 | `test_v32_r21_cross_task_reuse.py`、`test_v32_r3_full_system_self_tool_reuse.py` 同合同对齐与跨任务去重。 |
| D01/D02/D03/D04/D05 | `test_r1021_boundaries.py` terminal 原 RETURN、循环 local、下一 ACTION 禁止、坏 final effect、done 未 won、最后合法动作纯尾部；`test_r102_execution.py` terminal 后 0 在线请求。 |
| D06/D07/D08 | `test_r9_composite_deployment_evidence.py`、`test_r10_runtime.py` 当前 partial/后缀 skip、实际归属、rescue/infra/未执行不冒充图部署。 |
| D09 | `test_v32_r31_terminal_empirical_credit.py`、`test_r91_task_deployment_review.py` 原生命周期与维护阈值。 |
| D10 | `test_frozen_guards.py`、`test_provisional_promotion.py`、`test_r1021_boundaries.py` Frozen trial 本地性及训练收尾。 |
| E01/E02/E03 | `test_r1021_boundaries.py` Tool 附加效果失败、第二 Tool 真失败及实际回滚、正常成功；`test_v32_frozen_design.py` legacy source replay。 |
| E04/E05/E06 | `test_r1021_boundaries.py` typed JSON、整批写入与逐值覆盖；`test_r101_boundaries.py` Support/关系降级；`test_r3_semantic_anchors.py` 语义锚点与 revision。 |
| E07 | `test_r102_execution.py`、`test_r101_boundaries.py` Agent 连续探索、辅助返回、失败反馈均不自动抢占。 |
| F01/F02/F03/F06 | `test_r1021_boundaries.py` initial/rescue/continuation 实际工具调用，真实编译/解释/R1，一次 trial，Frozen policy 在调用前设置且长期 digest 不变。 |
| F04/F05 | `test_r10_runtime.py`、`test_r921_self_tooling_route.py`、`test_v32_r3_full_system_self_tool_reuse.py` 原 Candidate/两来源/cross replay/Active 边界。 |
| G01 | `test_r922_replay_certificates.py` authority 失效、同程序同 case 增量命中、新 case 回放。 |
| G02 | `test_v32_r4_learning_retention.py`、`test_r9_output_identity_lineage.py`、`test_v32_r21_cross_task_reuse.py` 角色改名及合同 identity。 |
| G03 | `test_r1021_boundaries.py::test_G03_live_atomicizer_rejects_implicit_legacy_boundary`；删除与反特判审计见配套报告。 |

### 基线反例与回归

独立基线副本 `/home/yangchengyu/asg_r1021_baseline_red_IGZO9Z` 仅承载测试，没有改动基线仓库或旧数据。将新生产入口测试放入原提交运行，6 个实质反例失败：A02、D01、E01、F01(initial/rescue/continuation)。日志：`runs/r1021_validation/baseline_red.log`。其余缺口以接口与源码差异核查，**不冒称全部已运行基线红测**。

中间 Windows `1421 passed`、WSL `1427 passed` 是工程过程记录，最终结果以以下固定提交验收为准。

最终实现锁定 `4679cd232040dc14b5c652ef7284e246d8ec1f3a`：Windows 全量 **1430 passed / 33.93s**，同提交 WSL 全量 **1430 passed / 37.57s**。后一日志来自隔离 checkout 的 `results/pytest.log`；未使用历史测试数字代替本版。

扩展基线核验保存在 `runs/r1021_validation/baseline_red_extended.log`：14 failed / 1 passed。其中 E06 逐值/JSON、E02 后续工具失败、C01 准备覆盖、F02 Dynamic trial 是实际旧行为反例；B01/E04 新 typed 接口缺失，A01 新 observer 模块缺失仅证明接口不具备，不冒充已执行整条新链。新版本对应场景均在上述全量中通过。

另用不导入新模块的实际 System/UsageLedger 资源探针验证 A01：同一节点已记 100,001 tokens 时，基线（100K-node/300K-task）返回 `node_tokens=0, task_tokens=199999`；锁定新版返回 `node_tokens_used=100001, task_tokens=499999`，下一真实 session 的 budget 为 499,999。探针使用受控 usage、未购买 API 请求，不将其冒称真实模型花费。

真实验收期间发现并修正的接口问题：E1 文本曾将当前来源端口 `authority.role` 与祖先审计字段 `authority.source_role` 混淆；改为明确读取前者，保留全部角色/值/scope 验证并增加反例。另明确每个提案 `phase_id` 唯一，不复制 Runtime owner ID。原失败提案与中间运行保留；没有降低验证条件使其准入。

## 真实验收计划与当前放行状态

- T1 使用更新后的 `experiments.r102_dataflow_check`，两合同与 fixture Active 明示，不冒充自然晋升。
- T3 与 dev16 使用 `experiments.run_v3_r102_targeted`，新 empty bank，固定任务顺序及 gamefile SHA256。
- T2/T4 从本次普通 E1/Builder/replay 与真实 terminal/ledger 记录核验，未触发不能算通过。
- T5 使用 `experiments.r1021_dynamic_check`，声明 no-graph 与不完整图的 Agent `plan_conflict` 定向入口，Dynamic 无假父接口；不计自主触发率。最初的 `give_up` 子例正确地未进入 rescue，其失败证据保留，不因此放宽生产条件。
- 所有原始失败保留。费用按唯一 usage event 与不可变 Trace 核对，Planner/Runtime/在线 Builder/离线学习/maintenance 分列；预算放宽不是 token 降低证据。

**最终状态：固定真实运行已经结束；没有遗留运行中的本轮验收进程，未启动 Full-120。** 工程回归、资产完整性、用量对账及正常收尾通过，未发现尚未修复的结构/权威/执行接线错误；但 T2 不能计作通过，因此保留路径验收缺口、暂缓完整放行确认，不以定向 fixture 或单元测试冒充该真实路径证据。

### 锁定实现的已完成实测

以下均来自 `4679cd232040dc14b5c652ef7284e246d8ec1f3a`，不拼接中间版本结果。费用包含任务内实际线上/离线调用及最终 maintenance，不含单列的 provider probe；reasoning 已包含在 completion，不重复相加。

| 项目 | 路径结果 | 用时 | 唯一 usage events | 实测总 tokens |
|---|---|---:|---:|---:|
| T1 | 通过：两次真实 Builder，原 RETURN→typed DataFlow→下游 0 次额外 Runtime；不是完整 benchmark 任务 | 59.38s | 4 | 28,977 |
| T2 补充定向 | 任务 won，但发现能力因错误语义约束被正常拒绝，**未通过目标路径验收** | 698.49s | 66 | 721,139 |
| T3 | 8/8 official won；真实来源→Candidate→部署→maintenance Active→独立任务成功复用 | 1,801.90s | 89 | 1,560,724 |
| T4 真实环境集成 | 通过：参数化声明 fixture，真实 API/ALFWorld，terminal→原 RETURN→真实部署信用；不是自主 Builder/路线成功 | 297.73s | 13 | 261,000 |
| T5 | initial / 合法 plan_conflict rescue 各一题，2/2 won，均真实自工具化及 trial | 687.54s | 23 | 507,210 |

以上五组 `all_usage.json` 与全部不可变 Trace 按 event ID 及内容核对一致，`audit_errors=[]`。T1 的 Active 资产、T4 的 Active 合同/参数化程序/准备序列、T5 的路由/菜单、T2 的 native-source 菜单与 E1 关注点均为事先声明的验收 fixture，不能当作自主触发率或自然晋升。

T3 的核心 ref 为 `skill://atomic_e3eb67090ec62106bfdaaa63@1.0.0`、`skill://impl_738d414238d551b9474ad076@1.0.0`、`tool://tool_2c142c83092424ccb62dc382@1.0.0`。task0 的 R0 拒绝、task7 的首次失败/回滚、task10 Active 调用失败均保留；task12 的 Active 调用实际完成四个内部动作并返回 `mug_2/cabinet_11`，不是仅以整个 task won 计为工具复用。该能力来自 Runtime automation，**不替代 T2 普通 E1 证据**。

T2 使用独立 fresh bank、固定 train6、一次尝试，普通 E1 提出四项，三项合法单步能力保留。发现能力把 `discovered_location=diningtable_2` 也声明为与目标 `bread` 兼容，因此正常报 `output semantic compatibility not certified: discovered_location`；不删约束或由代码替换输出救绿。E2 的离线预算耗尽同样保留，没有撤销三个合法 Atomic。该题 online Runtime 354,900 tokens 超过旧 300K 后成功，仅证明本轨迹的预算旁观事实，不构成 token 降低或旧轨迹反事实证明。

T4 的实际证据在 `t4_environment_complete/` 及 `t4_environment_audit.json`：Trace `trace_c2519bfacea044b190368483e0294d2d`，四个准备原生动作后，Tool 的第五个真实动作触发 official won；`executed_node_ids=[terminal_action, original_return]`、`completed=true`、`terminal_interrupted=false`，返回两项真实 input identity。图完整完成，Ledger 的 `evt_48e276d721073e21259da794d825fdd6db6fea7b0f338cec08adf817ace11311` 是实际 `deployment_success`，不是手填信用。terminal 后没有新增在线调用；合法离线学习/maintenance 正常继续，最终 audit/freeze 完成。必需后续 ACTION 不可跳过的对照由 D03 生产解释器回归覆盖。

固定自主开发集随后也自然覆盖了 T4，而非只依赖 fixture：train45 的 `trace_fe6d0f2fc8ca455886e3cec96677fad5` 选择 `skill://composite_970445c64f6f85eaba0705a7@1.0.0`，两次真实调用 `tool://tool_9feba820993ddbd10e4c9c5a@1.0.0`，第二次工具动作 `r021_a017` 触发 won，原 RETURN 返回 `soapbar_1/cabinet_2`；graph full/self-sufficient 均 true。真实 Ledger `evt_b75ec1d240ae1d6df058a0199d4e449a55852510d795ece29e019033ba084dde` 记录该图 deployment_success。此任务只计入 dev16 一次，不另加一个成功样本或重复计费。

不能隐藏的 T4 过程结果：最初真实 Builder 生成的 PUT 不在该场景当前 action catalog；其一次 Runtime 尝试失败并触达资源边界（69 个含 setup 的事件、616,629 tokens）。该诊断驱动还漏建了手工 Active Composite 的 CONTAINS 边，正确的完整性检查因此拒绝收尾；这是夹具错误，不修改生产检查。补充集成 fixture 首次静态检查缺 `expected_effects`，在任何 task action 前拒绝；第二次已真实 won/RETURN，但同样在缺 CONTAINS 的收尾处失败（11 事件、167,274 tokens）。补齐完整的声明 fixture 后得到上表 T4 结果。原 `t4/`、`t4_environment/`、`t4_environment_ready/` 的原始日志、Trace/usage 均保留，未回写 bank/Trace 或从失败运行接续。此处验证的是终止机制，不将手写验收程序冒称 Builder 学会了该工具，也没有重抽生成程序挑最好一次。

### 固定自主 dev16 最终结果

新 empty bank、锁定代码、固定 16 题依次运行完成，无补题/重抽/前缀拼接。**official won 14/16（87.5%）**；总墙钟 **12,313.53s（3小时25分13.53秒）**，包含加载、probe、实际任务、离线学习、replay 及收尾。`complete=true`、`code_unchanged=true`，16 题均 `infrastructure_failure=false`、`resource_usage_complete=true`。

| train ID（执行顺序） | official won | 在线 Runtime tokens | 逐题总 tokens | 题耗时 s |
|---|---|---:|---:|---:|
| 2 | true | 23,338 | 402,445 | 582.97 |
| 3 | true | 51,270 | 288,909 | 469.55 |
| 15 | true | 208,417 | 675,826 | 1,034.31 |
| 16 | true | 162,814 | 236,706 | 351.91 |
| 28 | true | 122,601 | 193,267 | 348.45 |
| 34 | true | 81,753 | 366,097 | 628.80 |
| 36 | false | 620,399 | 630,021 | 1,557.17 |
| 40 | true | 481,702 | 871,727 | 1,073.18 |
| 45 | true | 164,281 | 437,708 | 485.26 |
| 46 | true | 130,089 | 582,773 | 1,493.86 |
| 53 | true | 84,774 | 205,995 | 385.62 |
| 57 | false | 611,658 | 648,580 | 1,947.40 |
| 1 | true | 74,132 | 351,563 | 493.41 |
| 7 | true | 93,275 | 181,072 | 320.51 |
| 12 | true | 28,326 | 196,666 | 378.04 |
| 31 | true | 159,375 | 273,112 | 685.42 |

费用总计 **6,567,805 tokens / 412 唯一事件**；逐题合计 6,542,467，独立 maintenance 25,338。总账与任务/maintenance 不可变 Trace 的 event ID 集合及内容完全一致，未知 usage 为 0，`audit_errors=[]`。probe 的 5 请求 / 2,822 tokens 单列，不混入任务总量；含 probe 为 6,570,627。下表 reasoning 是 completion 的子集，不重复计费。

| 阶段 | 调用 | prompt | completion | 其中 reasoning | 非 reasoning completion | 总 tokens |
|---|---:|---:|---:|---:|---:|---:|
| Planner + ColdStart | 27 | 129,317 | 101,256 | 81,017 | 20,239 | 230,573 |
| Runtime（四个实际出现的在线桶） | 330 | 1,865,449 | 1,232,755 | 1,206,366 | 26,389 | 3,098,204 |
| 在线 ToolBuilder | 0 | 0 | 0 | 0 | 0 | 0 |
| E1 | 14 | 692,577 | 410,252 | 356,381 | 53,871 | 1,102,829 |
| E2 | 17 | 1,460,995 | 333,196 | 323,770 | 9,426 | 1,794,191 |
| 离线 ToolBuilder | 21 | 187,634 | 129,036 | 105,621 | 23,415 | 316,670 |
| maintenance | 3 | 21,733 | 3,605 | 2,864 | 741 | 25,338 |

实际底层动作共 **299**：原生 Agent 295、Agent 选中当前 Tool 内部 3、Agent 选中 helper 内部 1；自动图节点 Tool 动作 0、Runtime trial 动作 0。两次失败 invocation 都在执行环境动作前被拒绝，回滚范围均为空，物理恢复重放动作 0；失败请求和用量未删除。不要把 helper 菜单选择次数等同于实际产生动作的次数。逐 occurrence 费用、接口暴露、调用/preflight/started/completed、返回、状态时点、学习覆盖与 ledger 详见 `dev16_audit.json`。

最终资产：Atomic 13（Active 1/Candidate 12）、Composite 2（Active 1/Candidate 1）、Implementation 16 和 Tool 16（分别 Active 1/Candidate 11/Shadow 4）。最终 maintenance 无待处理提案，正常 artifact audit/freeze；源 digest 不变：`12eb475045ea3438fdce05bf0d75eba1c616521011dafa8229d60d60963898f5`。

#### 失败、拒绝与尚未覆盖部分

- **train36、57 未 won**：目标仍为 cup，Runtime 选择 mug 后重复处理/放置，最终 `runtime_task_token_budget_exhausted`。两题线上实际分别 620,399、611,658；最后一次响应跨安全上限被完整计费，未免费重开。公开 task goal 没有被代码改写，任务进度也没有把错误对象认成已满足；不加 cup/mug 特判补绿。
- **train12 official won，但合同不一致**：环境在 CLEAN mug 后即返回 done/won，轨迹只有四个动作，没有放到 coffeemachine；保留 `benchmark_goal_contract_mismatch`。官方成功仍按 won 统计，合同一致性独立记录，不恢复 strict-success 作为成功率，也不在 terminal 后补执行动作。
- train15、28、1 各一次尚未满足效果的提前验证被拒绝，随后正常 won；不抹掉可恢复失败记录。
- train31 的 Agent 两次提交 `station=countertop_1` 调用 CLEAN，当前 catalog 无该动作，正常拒绝；两层记录合计四条 failure，不是四次环境动作。Agent 后续用实际 sinkbasin 完成清洁，图后进入已有 task rescue，用一次原生放置赢得任务。图内 Runtime 151,893 加 Dynamic 7,482 共 159,375，切换未重置 task Ledger。没有自动将 station 改成别的地点。
- E1 共提出 82 个 occurrences、29 个被拒绝。主要是入口时点之后的 authority、无法认证的输出语义兼容性，以及 train31 把 `compatible_with_input` 写成自然语言解释而非实际 input role。保留原始提案，不解析自然语言替作者改约束或删除验证。
- **T2 最终仍未通过**：自主集唯一普通 E1 来源的三 ACTION 程序为 `tool://tool_734bce6595e5611aa80a2977@1.0.0`，在源 replay 的必需 GO_TO 不可用时拒绝，仍是 Shadow，不能算准入成功。没有已准入的普通 E1 多动作/循环程序来补齐该缺口；T3 的 Runtime automation 四动作复用不替代普通 E1 证据。
- 自主 dev16 没有触发在线 Builder/Runtime automation，自动节点 Tool 动作也是 0；T1/T3/T5 的定向覆盖不能当作自主频率。当前数据不支持“普遍降低 token”或“已达95%”结论。

旁观旧预算：train15、45、31 跨旧节点 100K 后 won；train40 跨旧任务 300K 后 won。train36、57 跨旧任务阈值后仍失败。详细首次 crossing event 与后续成本留在 `retired_limits`，不声称旧策略沿同一轨迹一定成功，不将放宽预算当效率收益。

结论：代码实现和工程验证已完成；真实路径 T1/T3/T4/T5 及 dev16 完整审计通过，**T2 的能力学习目标仍存在实测缺口，暂不提供“全部验收通过”的正式放行确认**。这不是已确认必须放宽验证器的代码错误；按冻结规范保留缺口，不继续重抽直到通过。是否在显式保留该缺口下进行正式方法评估，待用户确认；本轮没有自动启动正式训练。

### 身份与证据位置

- 实现 commit：`4679cd232040dc14b5c652ef7284e246d8ec1f3a`；后续仅 Markdown 报告提交不改变执行代码。
- execution/code hash：`305b714a10b9e861910be4e4aa976d5d5ffc094348f94896e86d55326c795ffa`。
- 该 execution hash 指实际 WSL 隔离 checkout 的 319 个清单文件字节，不是 Windows 工作区的通用 hash。Windows 有 12 个原有文件使用 CRLF，逐项差异仅为换行；Git 内容一致，但原始字节 hash 不同。未改写这些文件、未弱化 hash 验证；后续正式评估应使用新的 WSL Git checkout 和其实际 probe，不能搬用另一字节身份的 provider manifest。
- formal train config hash：`a4c932c51c245168b57f193668ed0ba6cf15c18ba623e92e3295a4c837c6ad41`。
- provider fingerprint：`9ae66a5844fdc307babbfa37be88d819c3ac26f7e0953da6285c237f1b9c7ed3`；锁定后的正式配置 probe 已通过、usage 完整。
- 正式配置 preflight 已通过：120 题、六类各 20，manifest hash `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640`；**未启动正式 train**。
- WSL 原始验收：`/home/yangchengyu/asg_r1021_locked_LR82Z1/results/`。
- Windows 副本与逐项 JSON 审计：`D:/T3S_exp/AtomicSkill-ToolGraph_v3/runs/r1021_validation/validated_4679cd2/`。各组包含原 Trace、bank、配置、manifest、provider 记录、进度和汇总；`*_audit.json` 包含逐题/逐 occurrence 成本、动作互斥分账、能力消费、学习覆盖、终止信用与原始 Ledger。
- 自主集汇总：上述目录的 `dev16/summary.json`；逐题进度 `dev16/progress.json`；原始轨迹 `dev16/trace_store/traces/`；完整审计 `dev16_audit.json`。诊断配置 hash 为 `cef301a74050ab73024a95991a8af3dc4ff7ef47f1eb70a0b35776d0eb5904c9`（隔离输出/诊断 phase 与正式配置不同），与本组 provider manifest 匹配。
- 只读审计脚本副本 `audit_completed.py` 的 SHA256 为 `d74d034b05264d763354d07dc94af32472f7ea09fa7dbcc30d0ab9a2efa2be0c`。运行结束后对九组 Windows 副本与 WSL 源逐文件核验；实际数据一致。dev16 的只读 SQLite 查询随后创建了临时 SHM/空 WAL，它们不属于实验数据差异；主数据库字节一致，源/副本 `integrity_check=ok`，均为 47 资产、337 证据事件。
- 中间版本分别保存在 `runs/r1021_validation/intermediate_9c4829e/`、`intermediate_c9fcf70/`、`intermediate_0510661/`、`intermediate_768d999/`；仅用于工程审计，不纳入最终固定开发集成功率或完整成本结论。

本次未跑同资源的预算-only 对照，不宣称方法带来的 token 减少；未跑正式 Frozen-134，不宣称达到 128/134 或 95%。

## R10.2.1-Final 收尾（2026-09-18；以下独立于前文历史结果）

基线 `4fcf70a4d151478c27c4da8c4e718046465fea1e`，执行文件基线 `4679cd2`。
实现提交 `74ea07fb919b0813a98a458cdd7cca1ad0217416`；公开嵌套错误文本补齐后最终执行提交
`1bdc1d3e38fc56fd3acb4440052e3e0e4c664463`。不新增协议开关，不改 600K/100 在线资源和 262,144 离线池。

### 改动与已完成工程验证

- F1：外部输入统一声明入口；support 按不可变事件索引排序，原提案不改；删除历史 validation 的全局否决及无范围授信；单项时间范围交给 subset；检查完整 canonical envelope 的真实 owner/parent/range/revision，允许同 occurrence 多个短 RuntimeStep。
- F2：一个逻辑 Extractor 的 E1/E2/E2R 各自 fresh session；阶段 ID/前序 ID 可审计，保留总共一次协议 repair；三桶离线用量在创建及每个实际请求前共读 task Ledger。最后超额响应完整收费，不再发起后续请求。E1 authority 只序列化一次。
- F3：Node 与所有 Dynamic 入口共用纯反馈投影，实际调用参数和 revision 不丢；嵌套失败/typed 输出/trial 反馈只取白名单。Implementation 子 Tool 的公开错误文本也保留，不复制 Tool body 或 validator 快照。没有新增自动选择、自动 Support 或参数替换。
- F4：共享稀疏 output_semantic_constraints 说明；明确精确 formal role、类别/身份与关系证据区别、入口/local/fresh 语义。验证器仍拒绝作者错误约束，没有自动删除。

首批七个真实旧行为反例在基线失败，见 `runs/r1021_final_validation/baseline_red_final.log`。
最终 Windows **1464 passed / 26.93s**；同提交独立 WSL **1464 passed / 36.79s**。
保留有效旧安全测试；迁移 fresh-stage fixture、authority 单处读取和不再全局否决的预期，未删除有效业务断言。
`git diff --check` 通过。新增定向文件 `tests/test_r1021_final.py` 共 34 个参数化测试实例；36 类语义验收复用有效生产回归，不要求另造 36 个同名函数。

### 36 类语义验收对应

以下 `Final` 指 `tests/test_r1021_final.py`；旧回归均包含在本提交全量测试中，不是沿用旧测试结果。

| 范围 | 生产断言位置/边界 |
|---|---|
| E01 | Final `test_E01_declared_entry_not_first_sparse_support` |
| E02–E04 | Final `test_E02_E03_E04_sparse_support_order_and_declared_entry`；原 E1 precondition authority 回归 |
| E05–E06 | Final `test_E05_E06_history_cannot_veto_or_grant_new_contract` 两 owner 情况 |
| E07 | Final `test_E07_historical_pass_cannot_certify_invalid_proposal` 四反例 |
| E08–E09 | Final `test_E08_invalid_sibling_range_is_subset_rejection`、`test_E09_global_schema_repair_all_rejected_and_empty_are_distinct`；R4 Atomic-only 保留回归 |
| E10 | Final `test_E10_real_runtime_steps_share_occurrence_not_span`：真实 TraceBuilder/两个 RuntimeStep→Normalizer→普通 typed Atomicizer |
| E11–E12 | Final parent/owner/range/orphan/cycle/unlearnable/sparse 回归；`test_r10_runtime` checkpoint/canonical rollback 回归 |
| X01–X02、X07 | Final `test_X01_X02_X07_fresh_stage_messages_authority_and_identity`；`test_extractor_e2_candidates` 有限 repair/冻结 candidates |
| X03 | Final `test_X03_allocation_includes_intervening_builder`：70K+120K→72,144 |
| X04–X05 | Final `test_X04_X05_shared_request_recheck_overrun_and_task_reset`；R91 Builder 预算 Atomic 保留回归 |
| X06 | `test_v32_r61_extractor_e2_repair`、`test_r91_tool_builder_budget_retention`：失败 E2/E2R 不撤销合法 Atomic |
| X08 | Final `test_X08_fresh_sessions_do_not_multiply_protocol_repair`；真实 Ledger/阶段 snapshot 对账 |
| R01、R06 | Final `test_R01_R06_dynamic_next_prompt_keeps_failed_call`：initial/rescue/continuation 的下一实际 provider prompt |
| R02 | Final `test_R02_preflight_feedback_survives_without_world_action` |
| R03–R04、R07 | Final `test_R03_R04_R07_feedback_is_public_but_preserves_typed_outputs`；原真实事务/typed publisher 正反例 |
| R05 | `test_r10_runtime` checkpoint 与 `test_r1021_boundaries` 串行 Tool 失败/费用不退；Final 保留 rollback 失败参数的投影断言 |
| R08 | Final `test_R08_node_next_prompt_has_actual_action_and_no_automatic_work`；`test_r102_execution` Agent 控制权回归 |
| I01、I04 | Final `test_I01_I04_sparse_constraints_validate_own_evidence`；typed boundary/Runtime draft 语义关系正反例 |
| I02–I03 | Final `test_I03_constraint_requires_actual_formal_input_role`；`test_r1021_boundaries` wrong_constraint 反例；原 T2 原始拒绝保留 |
| I05 | Final `test_I05_isomorphic_boundary_roles_values_and_source_ids` |
| I06 | `test_r1021_boundaries` D01–D05 terminal RETURN/后续 ACTION/false effect；`test_v32_r31_terminal_empirical_credit` |
| I07 | 原 typed publisher、逐值 ARGUMENT_EXISTS、`test_r101_replay_publication` orphan/证书回归 |
| I08 | `test_frozen_guards`、`test_v32_frozen_design`、Candidate/Active 与共享资源回归 |

### 原 Shadow Tool 重建与有限 T2 尝试

原 Tool `tool://tool_734bce6595e5611aa80a2977@1.0.0` 的源 Trace
`trace_5b721f702f5a443480913817c3ef045c`，原 E1 第二项入口 `[8,9)`、support `r008_a015`，
canonical/replay inclusive 范围 `[8,8]`；八个真实 prefix 动作与入口吻合，参数为
`object=candle_2, destination=cabinet_1`。game SHA256
`a6e637ffc4c437d6d994dcc3c79cbe6a9bc36f5b715eb491998f79d956967899`。
按原 prefix 重建后已在 cabinet_1，公开 catalog 无对应 GO_TO；原程序的首个必需 GO_TO 正确拒绝，
`started=false, executed_action_count=0, tool_ir_action_unavailable`，不是入口接线错误。不跳该动作、不改原程序、不提升 Shadow。

一次预先固定的旧 T2 train6 源 Trace `trace_9357bc1be64a4231b6a44a27df2ab226`，来源提交仍为 `4679cd2`。
原始文件 SHA256 为 `f9a1971535dc6908d29aa6ac96bc0a59c2b5a872d15d359f7796c9dc81d44db6`。
本次普通 E1 新调用 **101,789 tokens**，三项全部因引用该动作执行后才可用的 `action_arg` authority 作为入口 input 被拒绝；没有 Builder 调用、没有 T2 成功，不重抽。
原文件字节不改；新调用仅记入独立 `offline_source_review` Trace `trace_74ac18c598d64c25b171d59f0ab4ccc6`，不计作新 Runtime 成绩。
新审计 Trace SHA256 为 `08e9471f6e9897c494a1874065b990e06df56c48e9ca8f8f5fc276c3923aa679`；隔离目录中为 ReplaySourceAuthority 保存的旧源 Trace 是归档输入，旧 usage 不与本次新费用相加。
该尝试使用 `74ea07f`；之后 `1bdc1d3` 只补 Runtime 反馈文本及测试，E1/Builder/replay 文件未变，但不把旧 hash 隐称为最终 hash。
诊断驱动的加载准备问题（ToolRef/Tool JSON schema 包装、TraceStore 的治理型 dict loader）均在 E1 请求前发生，保留日志/probe费用，不把加载失败计成模型尝试，也不改生产加载器救验收。Frozen 预检驱动另有路径布局及误用 train-only manifest helper 的准备失败，修正的是隔离诊断驱动；没有据此改生产验证器。

### Final-only 中间验收（H 加入后停止，不属于联合最终结果）

中间独立 checkout：`/home/yangchengyu/asg_r1021_final_locked_B6ZkAF`。
实际执行字节 hash：`f550fefd304fdae79faf977b19b5d705113bdbd499366676903981d04dc1fd29`。

- 新 T1：通过，84.151s，4 次实际调用、34,582 tokens；真实 Builder 程序的 RETURN→typed DataFlow→下游零额外 Runtime。初始 Active 合同是已声明验收 fixture，不冒充普通 E1 学习或 benchmark won。Trace `trace_fe1cdbda1566424298252a2bd9b6edb5`，`t1_audit.json` 无对账错误。
- 新 Dynamic 反馈：通过，20.521s，2 次真实 Runtime 调用、5,306 tokens；第一次 helper 的 `tool_ir_action_unavailable`、原始输入 `target=desk_1`、before/after=0、started/completed=false 及公开文本 `no current OPEN affordance matches {'object': 'desk_1'}` 均进入第二次实际 provider prompt。之后 Agent 自己选择原生动作，共一次世界动作；无自动 Support。Trace `trace_aa81ab84fce84bb2b47028b4152423a6`，`dynamic_feedback_audit.json` 无对账错误。声明的初始 helper fixture 和两次决策后本地终止均记录在驱动；不宣称自主工具选择频率。
- 正式 train 配置预检：120 题、六类各 20，manifest `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640`，通过。
- 正式 frozen 配置预检：134 个真实 held-out 游戏与 train reference 身份/不相交检查通过，冻结知识 digest 不变，failure side 零读、provisional 零选择，`passed=true`。预检使用单独声明的空 fixture snapshot，并非已完成 Full120 的正式来源；未冒充正式 runner 的 completed-source 验证或 Frozen-134 成绩。

dev16 使用规定的 `2,3,15,16,28,34,36,40,45,46,53,57,1,7,12,31`，新 empty bank，未启动正式实验。
中间 `74ea07f` 的 T1/Dynamic 已通过、dev16 在发现公开错误文本遗漏后停止，均不拼入最终 dev16。
T2 若未在最终自主 dev16 自然闭环，维持“不完整放行”，不授予模型失败豁免。
本轮未证明 95% 成功率，也没有预算-only 对照，不将费用变化归因为算法提效。

H 加入时，该版本 dev16 已完成 5/16（2、3、15、16、28，均 won），正在执行 train34。
已停止旧进程，全部中间证据保存在 `runs/r1021_final_validation/intermediate_1bdc1d3/`；
不把已完成五题、进行中费用或旧 T1/Dynamic 拼接到 H 的新空库开发集。

## R10.2.1-H：生成约束与提示词联合收尾

实现提交：`eff547c0cbadb827643f8a133e0318fee53e7795`，`repair_revision` 仍为 R10.2.1。
H 是工作包名称，没有新增开关、Agent、checker 调用、重试池或预算。
执行 checkout：`/home/yangchengyu/asg_r1021_h_locked_mWrEZz`；代码字节 hash
`2fbc752bd09a24ae0f8d5f0ac677f63fc0c0bba24da17fba5f785562113051aa`。

### 有效约束的唯一位置

| 约束 | 新位置与保留边界 |
|---|---|
| E1 的 event_start、support、formal→source 显式映射 | `ContextBuilder.extractor_e1` 阶段说明＋原 boundary authority；未枚举 E1 将自声明的角色 |
| 稀疏输出兼容关系 | `OUTPUT_SEMANTIC_CONSTRAINT_RULES` 唯一定义，各相关阶段一次；schema 只保留短字段说明 |
| 已知 Atomic 身份、input/output 名、RETURN key | Builder 请求专用 schema 深拷贝；完整列表、类型、合同仍由原静态校验负责 |
| Tool ACTION 的 skill_input/constant/local_variable | 独立 `TOOL_ACTION_BINDING_SCHEMA`；图 BindingExpression 未改；local 生命周期由原编译器判断 |
| ACTION / RETURN 不同引用语法、loop、entry、evidence_outputs | Builder 单一阶段前缀的简短语法说明；原 parser/static/replay 均保留 |
| 原始 mandatory 源路径与 snapshot/live | Builder 前缀说明；没有新增“动作集合相等”“所有分支必须触发”判定 |
| E2/E2R 候选身份与组合职责 | 共用 `EXTRACTOR_COMPOSITE_PROMPT`；两 ID 空间各自 enum，空空间 maxItems=0；E2R 附加原一次修复职责 |
| Runtime 查询、原生动作、工具反馈 | `SEARCH_POLICY` 公共说明一次，Node/Dynamic 各自短前缀；精确反馈沿 Final 共用投影 |
| lazy draft 的能力定义、pre-trial 输出 | 仅实际 offered automation interface 时加载 `AUTOMATION_DRAFT_PROMPT`；完整 fresh-output 规则在公开 interface 一处 |
| Dynamic automation interface | 根 payload 保留完整对象，task_runtime_frame 中去掉完全相同副本；非修改输入数据 |
| terminal、typed 发布、证书、Candidate/Frozen | 原生产验证器及测试；没有以 prompt 或 schema 代替这些权限检查 |

Schema 仅收窄已类型化的浅层字段，不声称深层 loop/branch body 已全部被 native schema 严格约束。
没有启用 strict、response_format 或新 dialect。create 与完整 no_tool 都可提交；optional 输出仍 optional。
复制后的共享 ParameterSpec 子树显式解除 alias，避免输入/输出名相互污染及误限制 semantic_type。

### H 回归位置与验证范围

新增 `tests/test_r1021_h.py` 15 个参数化测试用例；有效旧回归保留，未删测试。

| H 编号 | 实际断言位置 |
|---|---|
| H01/H02/H05 | 新文件 `test_H01_H02_H05_e1_actual_request_and_new_formal_role`；Final E08/E09，boundaries B01/B02/C02 |
| H03/H04 | Final I01/I03、E01–E12；boundaries B01–B07 |
| H06/H09/H22 | 新文件 Builder 双请求隔离/完整 no_tool/实际调用数；Final X01–X08，原共享预算回归 |
| H07/H08/H20 | 新文件 ACTION/RETURN/optional/local 正反例，scope 外静态拒绝；原 Tool IR validator/runtime automation pre-trial 正例 |
| H10/H11 | `test_r922_runtime_boundaries` snapshot/live/limit、`test_r92_review_loop_scope`、`test_v32_frozen_design.test_program_paths_cover_branches`、R10/Final 源 replay 与 terminal；未改解释器、mandatory ACTION 不跳过 |
| H12 | 新文件 E2 双空间、空集合、base schema 不变；原 E2 semantic authority 测试 |
| H13 | Final `test_X01_X02_X07_fresh_stage_messages_authority_and_identity`、X08 与原 E2R 单次修复测试 |
| H14/H15 | 新文件 Node/initial/rescue/continuation × lazy offered/not offered 的八个实际请求；原消融/frozen guard |
| H16/H17 | Final R01–R08、I05 同构角色实体重命名；原 typed 输出/局部值测试 |
| H18 | 同形生产请求字节对照，见下一节 |
| H19 | 新文件实际 OpenAICompatibleProvider→HTTP mock→NativeToolSpec 往返两例：正常及一次原协议修复；不是付费 API 结果 |
| H21 | 原 terminal empirical credit、typed publication、orphan certificate、Frozen 全量回归 |

部分旧测试原先断言重复文本出现在多个位置，现改为断言唯一来源与引用；错误 Atomic ref 的伪造 fixture 改为真实 canonical ref，
非法 ID 的语义测试仍直接调用原语义边界验证错误码，同时新 schema 测试验证更早拒绝。没有降低生产拒绝标准。
Windows 全量 **1479 passed / 34.46s**；验证实际 import 路径后的独立 WSL 全量 **1479 passed / 33.13s**；diff check 通过。
验收启动时 `.env` 曾覆盖 PYTHONPATH 为 D 盘旧路径，使独立脚本缺少 experiments 导入。
当时尚无新 API 调用；初次测试/预检日志保留为 `setup_path_mismatch_*`，不算最终身份验证。
驱动改为 source `.env` 后显式设置 checkout 的 src/root/tests，并断言模块 `__file__` 位于该 checkout，再重做测试与预检。

### 同形请求字节对照（不是 provider tokens）

固定相同输入分别走 `1bdc1d3` 与 `eff547c` 的生产 session/context/native tool 构造路径。
证据：`runs/r1021_final_validation/h_request_byte_comparison.json`；完整前后请求在
`h_requests_before_complete/requests.json` 与 `h_requests_after_complete/requests.json`。
没有真实 API 费用；输入内容相等性检查仅允许移除 Dynamic 中与根相同的重复 interface。

| 请求 | 改前总字节 | 改后总字节 | 差值 |
|---|---:|---:|---:|
| E1 | 22222 | 15127 | -7095 |
| E2 | 5031 | 4829 | -202 |
| E2R | 3985 | 5505 | +1520 |
| Builder | 30947 | 24001 | -6946 |
| Node | 10630 | 9271 | -1359 |
| lazy Node | 22854 | 20473 | -2381 |
| initial / rescue / continuation（各） | 7820 | 7130 | -690 |
| lazy Dynamic | 29316 | 19434 | -9882 |

E2R 增长原因：原 repair 前缀只有修复提醒；独立会话现在收到完整共用 E2 职责再加修复信息。
不将这部分称作压缩收益，不将字节变化换算成真实费用或算法改进比例。

### H 中间真实验收（I 加入后停止，未放行）

同一固定 H commit 的 T1、Dynamic 公开失败反馈、预锁定旧 train6 普通 E1 T2、一次规定 dev16，全部使用隔离新 bank。
T2 来源 SHA256 与 Final 章节一致，不改源 Trace，不人工改模型提案，不用 Runtime automation/T3 替代。
T2 驱动读取 admission 中的成功 replay 记录（旧驱动只看失败侧路径的审计遗漏已修正，不涉及生产权限）。
H 的 T1 通过（37.056s、4 calls、22,085 tokens），Dynamic 反馈通过（17.895s、2 calls、4,328 tokens），两项独立账本审计均无错误。
固定旧 train6 的普通 E1 一次调用 111,207 tokens，5 项都引用入口后才产生的 action_arg/semantic_alias 作入口输入，全部拒绝，未进入 Builder。
该 T2 来源的旧学习注释中还含历史 applied/E2 预算状态；不得当成本次执行结果，当前结果以新 session/raw submission、5 条新拒绝和独立 usage 为准。
I 加入时停止 H dev16：完成 6/16（2、3、15、16、28、34），均 won；第 36 题进行中费用和现场一并保留，未计作完成题或完整对账。
H 全部中间结果在 `runs/r1021_final_validation/intermediate_eff547c/`，不能拼入新 I 版本。未启动 Full120/Frozen134。

## R10.2.1-I：源证据接线与接口一致性

生产修复 `e8fc880c22db8c82e2e3d3f3d991b7c8f03ec1e6`；验收固定提交
`699bacc3cc2d23bdc58933edaa27891a44392f27`（后一个提交只修历史 fixture 的缺省字段）。
执行目录 `/home/yangchengyu/asg_r1021_i_locked_Zyxf43`。

### 修复与审查

- 删除普通 Builder 对不存在的顶层 before_state_facts/after_state_facts 的读取。
- System 的一个私有纯 helper 按唯一、非 bool 整数 event_index 取声明入口；核对源 Trace/task、selected event 的参数/时点/Span 身份和 canonical prefix。
- 原 exact executable reuse 和零预算检查仍在 source helper 前。源字段缺失、格式错误、重复索引、身份冲突按 `builder_source_integrity` 基础设施错误停止，Builder 请求数为零；合法 recorded 空列表仍合法。
- `semantic_delta` 只包含 source_boundary、原 typed source_input_bindings、标准白名单 before_facts。没有 after_facts 完整末态暗示；不读 live Harness 补齐历史状态。
- support 保存真实顺序、index、原 before/after revision 及逐事件正负变化；负变化保留原 witness/revision，不伪造“否定事实”或完整终态。
- Builder 调用前复用原 `build_occurrence_replay_case` 的 source task/参数检查；该构造器、解释器、replay 权限和证书版本均未改。canonical 改名通过既有 role_map 比较 typed 值和同一 prefix，不机械比较新旧字段名。
- 用 I 文本替换 H 的 SOURCE AND REPLAY 段；E1 prompt/schema 的入口说明都改为 event_index 查找。补上原 `public_tool_ir_collection_sources()` 接口，collection/condition/RETURN 说明沿原接口和解释器，不新增 DSL。
- 未改模型、预算、retry、运行决策、自动 Support、lifecycle、正式配置或 baseline；没有对象/任务规则。

### 精简测试与生产断言

删除 `test_v32_r4_prompt_contracts.py` 中三个只做提示词 substring 断言的重复 R7 测试（b1/b2/b3）。
对应语义已由 H 的实际请求/新角色映射，以及 Final/I 的 authority、typed 边界正反例覆盖。
没有删除有效失败测试或按数量删测试。旧预算/历史静态拒绝 fixture 仅补齐真实 Normalizer 字段和源身份，不改拒绝结论，也不改原历史 JSON。
新增 `tests/test_r1021_i.py` 14 个参数化用例；Windows 全量 **1490 passed / 31.33s**，diff check 通过。

| I 编号 | 断言位置与结果边界 |
|---|---|
| S01/S02/S03/S06/S09/S10 | `test_S01_S02_S03_S06_S09_S10_system_request_uses_real_declared_entry` 三例：真实 native Trace/Normalizer，实际 System→受控 Provider；非空入口、晚 support、实际 rollback 稀疏索引、逆序 support、live 已在终点；native schema、源路径、collection 接口实际可见 |
| S04/S07/S08 | `test_S04_S07_S08_projection_empty_negative_and_json_types`：真实 P→Q delta 保持原 revision/witness；另测显式 recorded 空视图及 bool/int/string 不混淆 |
| S05/S17 | `test_S05_S17_corrupt_source_is_infrastructure_before_builder` 九种损坏，零新请求、infra 分类，不计 no_tool；原 ReplaySourceAuthority scope/篡改/回滚回归保留 |
| S11 | 原 `test_v32_frozen_design` branch paths、`test_r922_runtime_boundaries` selector/snapshot/live、ToolRunner 不可用 ACTION 与 terminal 正反例；解释器未改 |
| S12/S16/S18 | `test_S12_S16_S18_native_multistep_builder_compile_source_replay`：真实三步 native（第一步为 prefix）→普通验证合同→实际 Builder/编译→SourceAuthority/replay 完整两步成功；canonical 角色改名保留参数和 prefix；replay 零新 LLM |
| S13 | 原 Final I01/I03/E07、`test_r1021_boundaries`、typed publisher/ARGUMENT_EXISTS/witness/replay 正反例 |
| S14 | H create/no_tool/optional/local schema＋原 static scope；I no_tool 实际 System 请求，不注册假 Tool |
| S15 | `test_r1021_boundaries` F01–F06、Runtime R0/R1 及 H lazy/offered/pre-trial 正例，未要求 Runtime 有历史 source_boundary |

S12 是受控生成选择的生产接线回归，不冒充真实模型 T2。真实模型原生来源的 T2 仍须独立列出。

### 真实验收安排与交付边界

按用户最新说明：不以等满 16 题为本轮交付前提。固定 I checkout 上只开一次新空库 dev16，
并执行同身份 T1、预锁定旧 train6 T2、Dynamic 反馈及两正式配置预检；未运行正式 Full120/Frozen134。
若 dev16 仍在跑，交付时注明进度/PID/日志，后续由用户要求再分析全量；不把未完成的 freeze/汇总写成通过。
全部原始输出在 `results/`，Windows 可通过 `\\wsl.localhost\Ubuntu\home\yangchengyu\asg_r1021_i_locked_Zyxf43\results` 访问。
本次仍不声称 prompt-only 因果收益、不承诺 95%，也不将工程接线正确等同于普通 E1 多步实证或正式放行。

### I 本轮交付快照（2026-09-18 15:32:57 +08:00，开发集继续运行）

固定执行代码 hash：`4ff003321359c7657030c493113989dcec35585b01a1294af8a70c80859e6ff0`。
独立 WSL 全量 **1490 passed / 35.65s**；实际 import 位于上述锁定 checkout，provider capability 检查使用同一代码身份。
Train120 预检通过（六类各20，manifest hash `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640`）。
Frozen134 预检通过、零 failure-side read／provisional selected、digest 不变；这是声明的空 snapshot fixture 的配置与只读边界预检，**不是正式训练产物的134题性能实验**。

已结束的原始诊断、计量、审计和驱动已备份至 `runs/r1021_final_validation/validated_699bacc/`。
正在运行的 dev16 bank 未复制成所谓最终 snapshot，以免把活跃数据库备份冒充完整冻结审计。

| 真实目标 | 本轮结果 | 新增调用／tokens | 解释与审计边界 |
|---|---|---:|---|
| Dynamic helper失败公开反馈 | 通过，19.435s | 2 / 4,784 | helper失败进入下一真实请求，随后Agent原生动作；没有自动Support，独立计量审计无错误 |
| T1 typed图数据流 | **未通过**，141.789s | 6 / 53,163 | 上游Tool已返回typed位置/实体；下游生成的`gc_location_current`条件拒绝执行，随后Agent原生EXAMINE完成，未达到下游零额外Runtime验收 |
| T2 预锁定旧train6普通学习 | **未通过** | 1 / 107,135 | E1提出5项、5项均因入口输入authority不足被拒，Builder=0、准入资产=0；没有换源或反复抽样 |
| 固定dev16 | **2/16完成，2 won** | 已完成题203,471 tokens | train2=108,962、train3=94,509；15个E1候选均拒绝，普通Builder=0。train15进行中，不计入完成题费用或最终成功率 |

T1 原始生成包含额外的 `current_context / verifier_id=agent.at_location` grounding constraint，却没有该约束所需的有效动作参数绑定。
现有检查拒绝它，不删除条件、不替作者补值。该项按生成条件错误（G）保留；入口断点不是 I 源上下文缺失的证据。
T1 两个真实 Builder 调用均 native schema 接受并成功编译（create=2、no_tool=0）；Builder 使用20,427 tokens。
`t1_generation_funnel.json` 中通用 success-evolution 记录计数为0，不能拿它否定此手工合同定向驱动中实际编译的两项Tool；须联读原始 Builder文件及 `t1_audit.json`。
T2 属入口证据不足（E），不是新增Builder接线错误（W）；prompt/schema有效不保证模型提出合法来源。
该次 E1 费用为 prompt89,455＋completion17,680（其中reasoning14,453），不是再加一次reasoning。
源SHA仍为 `f9a1971535dc6908d29aa6ac96bc0a59c2b5a872d15d359f7796c9dc81d44db6`；`t2_attempt_audit.json` 验证源未变且新usage与新Trace相符。
旧源学习注释仅属历史，不能把它的applied/E2状态或费用算为本次结果。

普通 Builder 的生产请求捕获见 `validated_699bacc/i_bytes_after/request.json`：
由真实native TraceBuilder/Normalizer→System产生，不是手写semantic_delta；但 Provider受控、返回合法no_tool，**不是付费真实模型T2**。
其中同时保存canonical occurrence、输入、prefix、normalized source及实际messages/native tools。
入口为event1/revision1、prefix为event0；末support为event2/after revision3，live已在末态而入口仍取源revision1。
两步真实source replay及canonical改名后的typed绑定一致性由S12/S16/S18生产测试验证；
截至快照时间，尚无本轮真实普通E1→Builder请求可与成功admission配对，交付项这一实证部分仍待验证，不能用受控请求填成通过。

同形H→I请求对照见 `validated_699bacc/i_byte_comparison.json`（前后完整请求均备份）：
system160→160、user17,724→21,393、tools12,200→12,200，总计30,105→33,774字节，**增加3,669字节**。
增加的是缺失的源入口/参数/index/负delta及已有公共collection语法；原合同/schema和旧证据字段相等。
此对照无付费API、无实际token节省结论；真实模型费用另列于上表。

后台 dev16 PID **864610**，父启动脚本 PID **864572**；不会自动接着启动正式实验。
监控：`tail -f /home/yangchengyu/asg_r1021_i_locked_Zyxf43/results/dev16.log`。
运行后续的原始Trace、usage、summary以该锁定目录为准，本节只是上述时间的中途快照。
全16题、最终maintenance/资产与freeze对账尚未完成，按用户要求留待运行结束再分析。

**结论：I工程接线与现有生产不变量回归通过，未发现需放宽校验的依据；当前真实T1/T2门槛未通过，不能正式放行Full120/Frozen134。**
遵守I第10节停止标准，不为让坏提案通过追加规则、扩预算或修改运行中的代码。
