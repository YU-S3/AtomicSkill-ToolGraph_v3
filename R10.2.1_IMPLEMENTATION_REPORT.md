# R10.2.1 实施与验收

基线：`024957adfc368a2d4624b475046d5a39fedcc29c`。代码修复、全量工程回归、固定真实开发集及证据审计已完成；T1/T3/T4/T5 已通过目标路径，**T2 普通 E1 多步能力的真实准入仍未通过，暂不宣告完整放行**。本文件区分工程正确性、路径覆盖与方法效果。未改 baseline、旧 Trace/bank/证书，不接续旧运行前缀。

## 实施范围

| 包 | 生产修改 |
|---|---|
| A | `runtime/budget.py` 仅保留 task 100 个环境动作；`system.py`、`agents/session.py` 从同一 UsageLedger 计算六类在线桶共享 600K，在线 Builder 同池，离线独立；协议轮数错误不冒充 token 耗尽。跨旧阈值仅在 `runtime/r1021_metrics.py` 旁观记录。 |
| B | native E1 boundary v2 明确 input/output ParameterSpec、formal→来源角色、local refs、output constraints；`typed_boundary.py` 核验可用时点、Trace/occurrence 归属、类型及实际认证级别。canonical/signature/Builder 保留合同。普通入口拒绝 legacy；仅失败来源重放显式使用 legacy，当前晋升迁移为 v2。 |
| C | `ExtractionPolicy` 按 canonical accepted events 减去实际完成 Tool 的覆盖及已抽取事件，避免最后一个工具遮蔽准备工作；同 task 一次成功 E1，空 proposals 合法；已验证独立 Atomic 不因 E2 拒绝丢弃。 |
| D | `ToolRunner` 在原 PC/局部作用域内执行 terminal 后纯控制与 RETURN，遇下一 ACTION 停止，不跳过；`ImplementationRunner` 不能执行下一 Tool。当前 partial 与未来 skip 分开；`CreditAssigner` 使用真实图所有权及终止动作归属计算部署贡献，Tool partial 不获完整成功。 |
| E | Tool 全部 final effects 与串行 Implementation 完成共同构成成功闸门；三个结果类型携带实际 typed bindings；publishers 先整批验证再写入，无 raw fallback/整体权限升级；ARGUMENT_EXISTS 逐 JSON 类型和值验据。 |
| F | `task_runtime.py` 是既有 Dynamic 路径的 task-scope 接线，共用 InvocationCompiler、ToolRunner、事务、R0/Builder/static/trial/R1；无假父 Atomic、无额外额度，trial 不重复执行。node/task 晋升按各自实际完成条件、原两来源与 cross replay 阈值执行，Frozen 不持久化。 |

新配置：`configs/alfworld_train_full_120_r1021_seed42.yaml`、`configs/alfworld_frozen_eval_134_r1021_seed42.yaml`。模型/reasoning/output cap 不变；replay authority 唯一来源更新为 `tool_replay_r1021_v1`。

## 生产回归覆盖索引

以下 ID 对应冻结规范的语义场景，不以 pytest 总数代替断言。`tests/` 为下表默认目录。

| 规范 ID | 主要测试与断言来源 |
|---|---|
| A01/A03/A04/A06 | `test_r1021_boundaries.py::test_A01_A03_A04_all_online_buckets_share_one_task_pool`；六桶真实 usage、越界保留、下一 task 重置、离线不侵占；`test_r101_boundaries.py` 资源异常回滚不退款。 |
| A02 | `test_r1021_boundaries.py::test_A02_only_task_action_limit`，同节点 100 动作，第 101 拒绝。 |
| A05 | `test_deepseek_protocol.py`、`test_planner_phase_budget.py` 费用/重复 event/跨 task，reasoning 不重复计费。 |
| A07 | `test_r101_formal_boundaries.py`、`test_formal_guards.py`、`test_runtime_deepseek_repairs.py` 新配置合并、协议及资源错误。 |
| B01/B02/B03 | `test_r1021_boundaries.py` semantic 输入、内部公开实体、显式改名映射、错误来源/未来/local/owner 反例；`test_v32_e1_input_authority.py`。 |
| B04 | `test_v32_r3_semantic_snapshot_authority.py`、`test_v32_r7_semantic_alias_authority.py` entry 前置证据，不从后置状态补入口。 |
| B05 | `test_r9_output_identity_lineage.py`、`test_r921_witness_assignment.py`、`test_r1021_boundaries.py` 实际认证级别与联合输出 witness。 |
| B06 | `test_r92_review_loop_scope.py`、`test_r92_review_output_candidates.py`、`test_r1021_boundaries.py` 真解释器多步/循环/catalog 与失败关闭。 |
| B07 | `test_real_extractor_regressions.py`、`test_r102_execution.py` 实际 E2 数据边与上游返回驱动下游；T1 为对应真实验收。 |
| C01/C02/C03 | `test_r1021_boundaries.py` event 级覆盖、空提案、同一决策去重；`test_evolution_guards.py`、`test_deterministic_fullchain.py` 系统链路。 |
| C04 | `test_extractor_contract_authority.py`、`test_v32_r4_learning_retention.py`、`test_v32_r61_extractor_e2_repair.py` partial/empty/E2 拒绝下保留合法资产。 |
| C05 | `test_r10_runtime.py` sparse canonical/rollback；`test_provisional_promotion.py` 失败源隔离。 |
| C06 | `test_v32_r21_cross_task_reuse.py`、`test_v32_r3_full_system_self_tool_reuse.py` 同合同对齐与跨任务去重。 |
| D01/D02/D03/D04/D05 | `test_r1021_boundaries.py` terminal 原 RETURN、循环 local、下一 ACTION 禁止、坏 final effect、done 未 won、最后合法动作纯尾部；`test_r102_execution.py` terminal 后 0 在线请求。 |
| D06/D07/D08 | `test_r9_composite_deployment_evidence.py`、`test_r10_runtime.py` 当前 partial/后缀 skip、实际归属、rescue/infra/未执行不冒充图部署。 |
| D09 | `test_v32_r31_terminal_empirical_credit.py`、`test_r91_task_deployment_review.py` 原生命周期与维护阈值。 |
| D10 | `test_frozen_guards.py`、`test_provisional_promotion.py`、`test_r1021_boundaries.py` Frozen trial 本地性及训练收尾。 |
| E01/E02/E03 | `test_r1021_boundaries.py` Tool 附加效果失败、第二 Tool 真失败及实际回滚、正常成功；`test_v32_frozen_design.py` legacy source replay。 |
| E04/E05/E06 | `test_r1021_boundaries.py` typed JSON、整批写入与逐值覆盖；`test_r101_boundaries.py` Support/关系降级；`test_r3_semantic_anchors.py` 语义锚点与 revision。 |
| E07 | `test_r102_execution.py`、`test_r101_boundaries.py` Agent 连续探索、辅助返回、失败反馈均不自动抢占。 |
| F01/F02/F03/F06 | `test_r1021_boundaries.py` initial/rescue/continuation 实际工具调用，真实编译/解释/R1，一次 trial，Frozen policy 在调用前设置且长期 digest 不变。 |
| F04/F05 | `test_r10_runtime.py`、`test_r921_self_tooling_route.py`、`test_v32_r3_full_system_self_tool_reuse.py` 原 Candidate/两来源/cross replay/Active 边界。 |
| G01 | `test_r922_replay_certificates.py` authority 失效、同程序同 case 增量命中、新 case 回放。 |
| G02 | `test_v32_r4_learning_retention.py`、`test_r9_output_identity_lineage.py`、`test_v32_r21_cross_task_reuse.py` 角色改名及合同 identity。 |
| G03 | `test_r1021_boundaries.py::test_G03_live_atomicizer_rejects_implicit_legacy_boundary`；删除与反特判审计见配套报告。 |

### 基线反例与回归

独立基线副本 `/home/yangchengyu/asg_r1021_baseline_red_IGZO9Z` 仅承载测试，没有改动基线仓库或旧数据。将新生产入口测试放入原提交运行，6 个实质反例失败：A02、D01、E01、F01(initial/rescue/continuation)。日志：`runs/r1021_validation/baseline_red.log`。其余缺口以接口与源码差异核查，**不冒称全部已运行基线红测**。

中间 Windows `1421 passed`、WSL `1427 passed` 是工程过程记录，最终结果以以下固定提交验收为准。

最终实现锁定 `4679cd232040dc14b5c652ef7284e246d8ec1f3a`：Windows 全量 **1430 passed / 33.93s**，同提交 WSL 全量 **1430 passed / 37.57s**。后一日志来自隔离 checkout 的 `results/pytest.log`；未使用历史测试数字代替本版。

扩展基线核验保存在 `runs/r1021_validation/baseline_red_extended.log`：14 failed / 1 passed。其中 E06 逐值/JSON、E02 后续工具失败、C01 准备覆盖、F02 Dynamic trial 是实际旧行为反例；B01/E04 新 typed 接口缺失，A01 新 observer 模块缺失仅证明接口不具备，不冒充已执行整条新链。新版本对应场景均在上述全量中通过。

另用不导入新模块的实际 System/UsageLedger 资源探针验证 A01：同一节点已记 100,001 tokens 时，基线（100K-node/300K-task）返回 `node_tokens=0, task_tokens=199999`；锁定新版返回 `node_tokens_used=100001, task_tokens=499999`，下一真实 session 的 budget 为 499,999。探针使用受控 usage、未购买 API 请求，不将其冒称真实模型花费。

真实验收期间发现并修正的接口问题：E1 文本曾将当前来源端口 `authority.role` 与祖先审计字段 `authority.source_role` 混淆；改为明确读取前者，保留全部角色/值/scope 验证并增加反例。另明确每个提案 `phase_id` 唯一，不复制 Runtime owner ID。原失败提案与中间运行保留；没有降低验证条件使其准入。

## 真实验收计划与当前放行状态

- T1 使用更新后的 `experiments.r102_dataflow_check`，两合同与 fixture Active 明示，不冒充自然晋升。
- T3 与 dev16 使用 `experiments.run_v3_r102_targeted`，新 empty bank，固定任务顺序及 gamefile SHA256。
- T2/T4 从本次普通 E1/Builder/replay 与真实 terminal/ledger 记录核验，未触发不能算通过。
- T5 使用 `experiments.r1021_dynamic_check`，声明 no-graph 与不完整图的 Agent `plan_conflict` 定向入口，Dynamic 无假父接口；不计自主触发率。最初的 `give_up` 子例正确地未进入 rescue，其失败证据保留，不因此放宽生产条件。
- 所有原始失败保留。费用按唯一 usage event 与不可变 Trace 核对，Planner/Runtime/在线 Builder/离线学习/maintenance 分列；预算放宽不是 token 降低证据。

**最终状态：固定真实运行已经结束；没有遗留运行中的本轮验收进程，未启动 Full-120。** 工程回归、资产完整性、用量对账及正常收尾通过，未发现尚未修复的结构/权威/执行接线错误；但 T2 不能计作通过，因此保留路径验收缺口、暂缓完整放行确认，不以定向 fixture 或单元测试冒充该真实路径证据。

### 锁定实现的已完成实测

以下均来自 `4679cd232040dc14b5c652ef7284e246d8ec1f3a`，不拼接中间版本结果。费用包含任务内实际线上/离线调用及最终 maintenance，不含单列的 provider probe；reasoning 已包含在 completion，不重复相加。

| 项目 | 路径结果 | 用时 | 唯一 usage events | 实测总 tokens |
|---|---|---:|---:|---:|
| T1 | 通过：两次真实 Builder，原 RETURN→typed DataFlow→下游 0 次额外 Runtime；不是完整 benchmark 任务 | 59.38s | 4 | 28,977 |
| T2 补充定向 | 任务 won，但发现能力因错误语义约束被正常拒绝，**未通过目标路径验收** | 698.49s | 66 | 721,139 |
| T3 | 8/8 official won；真实来源→Candidate→部署→maintenance Active→独立任务成功复用 | 1,801.90s | 89 | 1,560,724 |
| T4 真实环境集成 | 通过：参数化声明 fixture，真实 API/ALFWorld，terminal→原 RETURN→真实部署信用；不是自主 Builder/路线成功 | 297.73s | 13 | 261,000 |
| T5 | initial / 合法 plan_conflict rescue 各一题，2/2 won，均真实自工具化及 trial | 687.54s | 23 | 507,210 |

以上五组 `all_usage.json` 与全部不可变 Trace 按 event ID 及内容核对一致，`audit_errors=[]`。T1 的 Active 资产、T4 的 Active 合同/参数化程序/准备序列、T5 的路由/菜单、T2 的 native-source 菜单与 E1 关注点均为事先声明的验收 fixture，不能当作自主触发率或自然晋升。

T3 的核心 ref 为 `skill://atomic_e3eb67090ec62106bfdaaa63@1.0.0`、`skill://impl_738d414238d551b9474ad076@1.0.0`、`tool://tool_2c142c83092424ccb62dc382@1.0.0`。task0 的 R0 拒绝、task7 的首次失败/回滚、task10 Active 调用失败均保留；task12 的 Active 调用实际完成四个内部动作并返回 `mug_2/cabinet_11`，不是仅以整个 task won 计为工具复用。该能力来自 Runtime automation，**不替代 T2 普通 E1 证据**。

T2 使用独立 fresh bank、固定 train6、一次尝试，普通 E1 提出四项，三项合法单步能力保留。发现能力把 `discovered_location=diningtable_2` 也声明为与目标 `bread` 兼容，因此正常报 `output semantic compatibility not certified: discovered_location`；不删约束或由代码替换输出救绿。E2 的离线预算耗尽同样保留，没有撤销三个合法 Atomic。该题 online Runtime 354,900 tokens 超过旧 300K 后成功，仅证明本轨迹的预算旁观事实，不构成 token 降低或旧轨迹反事实证明。

T4 的实际证据在 `t4_environment_complete/` 及 `t4_environment_audit.json`：Trace `trace_c2519bfacea044b190368483e0294d2d`，四个准备原生动作后，Tool 的第五个真实动作触发 official won；`executed_node_ids=[terminal_action, original_return]`、`completed=true`、`terminal_interrupted=false`，返回两项真实 input identity。图完整完成，Ledger 的 `evt_48e276d721073e21259da794d825fdd6db6fea7b0f338cec08adf817ace11311` 是实际 `deployment_success`，不是手填信用。terminal 后没有新增在线调用；合法离线学习/maintenance 正常继续，最终 audit/freeze 完成。必需后续 ACTION 不可跳过的对照由 D03 生产解释器回归覆盖。

固定自主开发集随后也自然覆盖了 T4，而非只依赖 fixture：train45 的 `trace_fe6d0f2fc8ca455886e3cec96677fad5` 选择 `skill://composite_970445c64f6f85eaba0705a7@1.0.0`，两次真实调用 `tool://tool_9feba820993ddbd10e4c9c5a@1.0.0`，第二次工具动作 `r021_a017` 触发 won，原 RETURN 返回 `soapbar_1/cabinet_2`；graph full/self-sufficient 均 true。真实 Ledger `evt_b75ec1d240ae1d6df058a0199d4e449a55852510d795ece29e019033ba084dde` 记录该图 deployment_success。此任务只计入 dev16 一次，不另加一个成功样本或重复计费。

不能隐藏的 T4 过程结果：最初真实 Builder 生成的 PUT 不在该场景当前 action catalog；其一次 Runtime 尝试失败并触达资源边界（69 个含 setup 的事件、616,629 tokens）。该诊断驱动还漏建了手工 Active Composite 的 CONTAINS 边，正确的完整性检查因此拒绝收尾；这是夹具错误，不修改生产检查。补充集成 fixture 首次静态检查缺 `expected_effects`，在任何 task action 前拒绝；第二次已真实 won/RETURN，但同样在缺 CONTAINS 的收尾处失败（11 事件、167,274 tokens）。补齐完整的声明 fixture 后得到上表 T4 结果。原 `t4/`、`t4_environment/`、`t4_environment_ready/` 的原始日志、Trace/usage 均保留，未回写 bank/Trace 或从失败运行接续。此处验证的是终止机制，不将手写验收程序冒称 Builder 学会了该工具，也没有重抽生成程序挑最好一次。

### 固定自主 dev16 最终结果

新 empty bank、锁定代码、固定 16 题依次运行完成，无补题/重抽/前缀拼接。**official won 14/16（87.5%）**；总墙钟 **12,313.53s（3小时25分13.53秒）**，包含加载、probe、实际任务、离线学习、replay 及收尾。`complete=true`、`code_unchanged=true`，16 题均 `infrastructure_failure=false`、`resource_usage_complete=true`。

| train ID（执行顺序） | official won | 在线 Runtime tokens | 逐题总 tokens | 题耗时 s |
|---|---|---:|---:|---:|
| 2 | true | 23,338 | 402,445 | 582.97 |
| 3 | true | 51,270 | 288,909 | 469.55 |
| 15 | true | 208,417 | 675,826 | 1,034.31 |
| 16 | true | 162,814 | 236,706 | 351.91 |
| 28 | true | 122,601 | 193,267 | 348.45 |
| 34 | true | 81,753 | 366,097 | 628.80 |
| 36 | false | 620,399 | 630,021 | 1,557.17 |
| 40 | true | 481,702 | 871,727 | 1,073.18 |
| 45 | true | 164,281 | 437,708 | 485.26 |
| 46 | true | 130,089 | 582,773 | 1,493.86 |
| 53 | true | 84,774 | 205,995 | 385.62 |
| 57 | false | 611,658 | 648,580 | 1,947.40 |
| 1 | true | 74,132 | 351,563 | 493.41 |
| 7 | true | 93,275 | 181,072 | 320.51 |
| 12 | true | 28,326 | 196,666 | 378.04 |
| 31 | true | 159,375 | 273,112 | 685.42 |

费用总计 **6,567,805 tokens / 412 唯一事件**；逐题合计 6,542,467，独立 maintenance 25,338。总账与任务/maintenance 不可变 Trace 的 event ID 集合及内容完全一致，未知 usage 为 0，`audit_errors=[]`。probe 的 5 请求 / 2,822 tokens 单列，不混入任务总量；含 probe 为 6,570,627。下表 reasoning 是 completion 的子集，不重复计费。

| 阶段 | 调用 | prompt | completion | 其中 reasoning | 非 reasoning completion | 总 tokens |
|---|---:|---:|---:|---:|---:|---:|
| Planner + ColdStart | 27 | 129,317 | 101,256 | 81,017 | 20,239 | 230,573 |
| Runtime（四个实际出现的在线桶） | 330 | 1,865,449 | 1,232,755 | 1,206,366 | 26,389 | 3,098,204 |
| 在线 ToolBuilder | 0 | 0 | 0 | 0 | 0 | 0 |
| E1 | 14 | 692,577 | 410,252 | 356,381 | 53,871 | 1,102,829 |
| E2 | 17 | 1,460,995 | 333,196 | 323,770 | 9,426 | 1,794,191 |
| 离线 ToolBuilder | 21 | 187,634 | 129,036 | 105,621 | 23,415 | 316,670 |
| maintenance | 3 | 21,733 | 3,605 | 2,864 | 741 | 25,338 |

实际底层动作共 **299**：原生 Agent 295、Agent 选中当前 Tool 内部 3、Agent 选中 helper 内部 1；自动图节点 Tool 动作 0、Runtime trial 动作 0。两次失败 invocation 都在执行环境动作前被拒绝，回滚范围均为空，物理恢复重放动作 0；失败请求和用量未删除。不要把 helper 菜单选择次数等同于实际产生动作的次数。逐 occurrence 费用、接口暴露、调用/preflight/started/completed、返回、状态时点、学习覆盖与 ledger 详见 `dev16_audit.json`。

最终资产：Atomic 13（Active 1/Candidate 12）、Composite 2（Active 1/Candidate 1）、Implementation 16 和 Tool 16（分别 Active 1/Candidate 11/Shadow 4）。最终 maintenance 无待处理提案，正常 artifact audit/freeze；源 digest 不变：`12eb475045ea3438fdce05bf0d75eba1c616521011dafa8229d60d60963898f5`。

#### 失败、拒绝与尚未覆盖部分

- **train36、57 未 won**：目标仍为 cup，Runtime 选择 mug 后重复处理/放置，最终 `runtime_task_token_budget_exhausted`。两题线上实际分别 620,399、611,658；最后一次响应跨安全上限被完整计费，未免费重开。公开 task goal 没有被代码改写，任务进度也没有把错误对象认成已满足；不加 cup/mug 特判补绿。
- **train12 official won，但合同不一致**：环境在 CLEAN mug 后即返回 done/won，轨迹只有四个动作，没有放到 coffeemachine；保留 `benchmark_goal_contract_mismatch`。官方成功仍按 won 统计，合同一致性独立记录，不恢复 strict-success 作为成功率，也不在 terminal 后补执行动作。
- train15、28、1 各一次尚未满足效果的提前验证被拒绝，随后正常 won；不抹掉可恢复失败记录。
- train31 的 Agent 两次提交 `station=countertop_1` 调用 CLEAN，当前 catalog 无该动作，正常拒绝；两层记录合计四条 failure，不是四次环境动作。Agent 后续用实际 sinkbasin 完成清洁，图后进入已有 task rescue，用一次原生放置赢得任务。图内 Runtime 151,893 加 Dynamic 7,482 共 159,375，切换未重置 task Ledger。没有自动将 station 改成别的地点。
- E1 共提出 82 个 occurrences、29 个被拒绝。主要是入口时点之后的 authority、无法认证的输出语义兼容性，以及 train31 把 `compatible_with_input` 写成自然语言解释而非实际 input role。保留原始提案，不解析自然语言替作者改约束或删除验证。
- **T2 最终仍未通过**：自主集唯一普通 E1 来源的三 ACTION 程序为 `tool://tool_734bce6595e5611aa80a2977@1.0.0`，在源 replay 的必需 GO_TO 不可用时拒绝，仍是 Shadow，不能算准入成功。没有已准入的普通 E1 多动作/循环程序来补齐该缺口；T3 的 Runtime automation 四动作复用不替代普通 E1 证据。
- 自主 dev16 没有触发在线 Builder/Runtime automation，自动节点 Tool 动作也是 0；T1/T3/T5 的定向覆盖不能当作自主频率。当前数据不支持“普遍降低 token”或“已达95%”结论。

旁观旧预算：train15、45、31 跨旧节点 100K 后 won；train40 跨旧任务 300K 后 won。train36、57 跨旧任务阈值后仍失败。详细首次 crossing event 与后续成本留在 `retired_limits`，不声称旧策略沿同一轨迹一定成功，不将放宽预算当效率收益。

结论：代码实现和工程验证已完成；真实路径 T1/T3/T4/T5 及 dev16 完整审计通过，**T2 的能力学习目标仍存在实测缺口，暂不提供“全部验收通过”的正式放行确认**。这不是已确认必须放宽验证器的代码错误；按冻结规范保留缺口，不继续重抽直到通过。是否在显式保留该缺口下进行正式方法评估，待用户确认；本轮没有自动启动正式训练。

### 身份与证据位置

- 实现 commit：`4679cd232040dc14b5c652ef7284e246d8ec1f3a`；后续仅 Markdown 报告提交不改变执行代码。
- execution/code hash：`305b714a10b9e861910be4e4aa976d5d5ffc094348f94896e86d55326c795ffa`。
- 该 execution hash 指实际 WSL 隔离 checkout 的 319 个清单文件字节，不是 Windows 工作区的通用 hash。Windows 有 12 个原有文件使用 CRLF，逐项差异仅为换行；Git 内容一致，但原始字节 hash 不同。未改写这些文件、未弱化 hash 验证；后续正式评估应使用新的 WSL Git checkout 和其实际 probe，不能搬用另一字节身份的 provider manifest。
- formal train config hash：`a4c932c51c245168b57f193668ed0ba6cf15c18ba623e92e3295a4c837c6ad41`。
- provider fingerprint：`9ae66a5844fdc307babbfa37be88d819c3ac26f7e0953da6285c237f1b9c7ed3`；锁定后的正式配置 probe 已通过、usage 完整。
- 正式配置 preflight 已通过：120 题、六类各 20，manifest hash `5d3b3d1ef4ce891c2bfaf531f942a9f943caff3c3ac64b8843cee5aae27a1640`；**未启动正式 train**。
- WSL 原始验收：`/home/yangchengyu/asg_r1021_locked_LR82Z1/results/`。
- Windows 副本与逐项 JSON 审计：`D:/T3S_exp/AtomicSkill-ToolGraph_v3/runs/r1021_validation/validated_4679cd2/`。各组包含原 Trace、bank、配置、manifest、provider 记录、进度和汇总；`*_audit.json` 包含逐题/逐 occurrence 成本、动作互斥分账、能力消费、学习覆盖、终止信用与原始 Ledger。
- 自主集汇总：上述目录的 `dev16/summary.json`；逐题进度 `dev16/progress.json`；原始轨迹 `dev16/trace_store/traces/`；完整审计 `dev16_audit.json`。诊断配置 hash 为 `cef301a74050ab73024a95991a8af3dc4ff7ef47f1eb70a0b35776d0eb5904c9`（隔离输出/诊断 phase 与正式配置不同），与本组 provider manifest 匹配。
- 只读审计脚本副本 `audit_completed.py` 的 SHA256 为 `d74d034b05264d763354d07dc94af32472f7ea09fa7dbcc30d0ab9a2efa2be0c`。运行结束后对九组 Windows 副本与 WSL 源逐文件核验；实际数据一致。dev16 的只读 SQLite 查询随后创建了临时 SHM/空 WAL，它们不属于实验数据差异；主数据库字节一致，源/副本 `integrity_check=ok`，均为 47 资产、337 证据事件。
- 中间版本分别保存在 `runs/r1021_validation/intermediate_9c4829e/`、`intermediate_c9fcf70/`、`intermediate_0510661/`、`intermediate_768d999/`；仅用于工程审计，不纳入最终固定开发集成功率或完整成本结论。

本次未跑同资源的预算-only 对照，不宣称方法带来的 token 减少；未跑正式 Frozen-134，不宣称达到 128/134 或 95%。
